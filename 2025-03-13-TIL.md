# 2024-03-13-TIL
**이중 포인터(Double Pointer)란?**

이중 포인터(double pointer)란 포인터를 가리키는 포인터를 의미하며, `**`을 사용하여 선언한다. 이중 포인터는 다차원 배열, 포인터 배열, 함수에서 포인터를 수정할 때 사용된다.

---

### 1. 이중 포인터의 선언과 기본 개념
```c
int a = 10;
int *p = &a;   // 포인터 p는 a의 주소를 저장
int **pp = &p; // 이중 포인터 pp는 p의 주소를 저장
```
위 코드에서:
- `a`는 정수 변수
- `p`는 `a`를 가리키는 포인터
- `pp`는 `p`를 가리키는 이중 포인터

각 변수의 값과 주소 관계:
```
&a  -> 0x1000 (a의 주소)
&p  -> 0x2000 (p의 주소)
&pp -> 0x3000 (pp의 주소)
p   -> 0x1000 (a의 주소 저장)
pp  -> 0x2000 (p의 주소 저장)
*p  -> 10 (a의 값)
**pp -> 10 (a의 값)
```

---

### 2. 포인터를 함수에서 변경하기
일반 포인터를 함수 인자로 전달하면 값이 변경되지 않지만, 이중 포인터를 사용하면 원래 포인터 값을 변경할 수 있다.

```c
#include <stdio.h>

void changePointer(int **pp) {
    static int b = 20;
    *pp = &b; // p가 새로운 주소를 가리키도록 변경
}

int main() {
    int a = 10;
    int *p = &a;
    printf("Before: %d\n", *p);
    
    changePointer(&p);
    printf("After: %d\n", *p);
    
    return 0;
}
```
출력 결과:
```
Before: 10
After: 20
```

---

### 3. 이중 포인터와 포인터 배열
이중 포인터를 활용하면 포인터 배열을 다룰 수 있다.

```c
#include <stdio.h>

int main() {
    char *names[] = {"Apple", "Banana", "Cherry"};
    char **ptr = names; // 이중 포인터로 배열을 가리킴
    
    for (int i = 0; i < 3; i++) {
        printf("%s\n", *(ptr + i));
    }
    
    return 0;
}
```
출력 결과:
```
Apple
Banana
Cherry
```

---

### 4. 주의할 점
1. **이중 포인터를 사용할 때 올바르게 사용해야 함**
2. **포인터 연산을 조심해야 함**
   - `*pp`와 `**pp`의 차이를 정확히 이해해야 한다.
3. **포인터를 잘못된 주소로 변경하지 않도록 주의**
   - `NULL` 체크 후 접근하는 것이 안전하다.

이중 포인터는 포인터 수정 등에 강력한 도구가 될 수 있으므로, 적절하게 활용하면 효율적인 코드 작성이 가능하다.

