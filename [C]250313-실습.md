```c
#include <stdio.h>
#include <stdlib.h>
#include <limits.h>
//#include <math.h>

#define ROWS 3
#define COLS 3

int len(char* str);
void strtoupper(char* str);
double strtolower(char* str, int a);

void compare_ascending(const int* a, const int* b);
void compare_descending(const int* a, const int* b);

void f_001();
void f_002();
void f_003();
void f_004();

void add(const int* ptr[]);
void max_arr(const int* ptr[]);
void min_arr(const int* ptr[]);
void pow(const int* ptr[]);
void f_005();
int main() {
	//f_001();
	//f_002();
	//f_003();
	f_004();
	//f_005();
	return 0;
}

// 이중포인터 3번 문제
void f_001() {
	static int arr[3][3];
	int* ptr[3] = { arr[0], arr[1], arr[2] };
	static int newArr[3][3];

	// 입력 받기
	printf("3x3 행렬 입력: \n");
	for (int i = 0; i < 3; i++) {
		for (int j = 0; j < 3; j++) {
			scanf_s("%d", &ptr[i][j]);
		}
	}

	// 함수 회전
	// arr[0][2] <= arr[0][0] => 
	// arr[0][1] <= arr[1][0]
	// arr[0][0] <= arr[2][0]

	for (int i = 0; i < 3; i++) {
		for (int j = 2; j >= 0; j--) {
			int temp = 
			newArr[i][j] = arr[2 - j][i];
		}
	}

	for (int i = 0; i < 3; i++) {
		for (int j = 0; j < 3; j++) {
			printf("%d ", newArr[i][j]);
		}
		printf("\n");
	}

}

// 함수 포인터 2번 문제
void f_002() {
	int cmd;
	char str[30];
	double (*fpmm)(char* str); // 함수 포인터 선언

	printf("1. 문자열 길이 구하기\n");
	printf("2. 대문자로 변환하기\n");
	printf("3. 소문자로 변환하기\n");

	scanf_s("%d", &cmd);

	printf("문자열 입력: ");
	scanf_s("%s", str, (unsigned int)30);

	switch (cmd) {
	case 1:
		printf("문자열 길이: %d\n", len(str)); // 길이 출력 후 종료
		return 0;
	case 2:
		fpmm = strtoupper;
		break;
	case 3:
		fpmm = strtolower;
		break;
	default:
		printf("잘못된 입력입니다.\n");
		return 1;
	}

	fpmm(str); // 문자열 변환 함수 호출

}

// 문자열 길이를 반환하는 함수
int len(char* str) {
	int cnt = 0;
	while (*str != '\0') {
		str++;
		cnt++;
	}
	return cnt; // 길이 반환
}

// 문자열을 대문자로 변환하는 함수
void strtoupper(char* str) {
	char* tmp = str; // 원래 주소 저장
	while (*str != '\0') {
		if ('a' <= *str && *str <= 'z') *str -= 32;
		str++;
	}
	printf("변환된 문자열: %s\n", tmp);
}

// 문자열을 소문자로 변환하는 함수
double strtolower(char* str, int a) {
	char* tmp = str; // 원래 주소 저장
	while (*str != '\0') {
		if ('A' <= *str && *str <= 'Z') *str += 32;
		str++;
	}
	printf("변환된 문자열: %s\n", tmp);

	return 123.12;
}

// 함수 포인터 3번 문제
void f_003() {
	int arr[5];
	int cmd;
	void (*func)(const int* a, const int* b);

	printf("배열을 입력하세요 (5개의 정수): ");
	for (int i = 0; i < 5; i++) {
		scanf_s("%d", &arr[i]);
	}

	printf("1. 오름차순\n");
	printf("2. 내림차순\n");
	printf("선택 : ");
	scanf_s("%d", &cmd);

	// 만약 cmd가 1번이면 오름차순, 2번이면 내림차순
	if (cmd == 1) {
		func = compare_ascending;
		qsort(arr, 5, sizeof(int), func);
		
	}
	else if (cmd == 2) {
		func = compare_descending;
		qsort(arr, 5, sizeof(int), func);
	}

	for (int i = 0; i < sizeof(arr) / sizeof(int); i++) {
		printf("%d ", *(arr + i));
	}
	
}

void compare_ascending(const int* a, const int* b) {
	return(*a - *b); // 오름차순
};
void compare_descending(const int* a, const int* b) {
	return (*b - *a); // 내림차순
};

// 이중포인터 추가 문제 1
void f_004() {
	// 행과 열 입력
	int r, c;
	int arr[5][5];
	int del_r;


	printf("행과 열의 크기 입력 : ");
	scanf_s("%d %d", &r, &c);
	getchar();

	// 행, 열 값 입력
	for (int i = 0; i < r; i++) {
		for (int j = 0; j < c; j++) {
			arr[i][j] = (10 * i) + j;
		}
	}

	printf("배열의 초기값은 자동으로 입력됩니다.\n");
	for (int i = 0; i < r; i++) {
		for (int j = 0; j < c; j++) {
			printf("%d ", arr[i][j]);
		}
		printf("\n");
	}

	printf("삭제하려는 행의 인덱스 번호를 입력하세요 : ");
	scanf_s("%d", &del_r);


	printf("수정된 배열 출력\n");

	for (int i = del_r+1; i < r; i++) {
		for (int j = 0; j < c; j++) {
			arr[i - 1][j] = arr[i][j];
		}
	}

	for (int i = 0; i < r-1; i++) {
		for (int j = 0; j < c; j++) {
			printf("%d ", arr[i][j]);
		}
		printf("\n");
	}
	

}

// 이중포인터 추가 문제 2
void f_005() {

	int array[ROWS][COLS] = {
		{0, 1, 2},
		{3, 4, 5},
		{6, 7, 8}
	};
	int cmd;

	// 이중포인터를 활용해 해결
	int* ptr[3] = {array[0], array[1], array[2]};

	printf("연산 방법 선택하기\n");
	printf("0: 합, 1: 최대값, 2: 최소값, 3: 제곱 >>");
	scanf_s("%d", &cmd);

	void (*fptr)(int* ptr);

	switch (cmd) {
	case 0:
		fptr = add;
		//add(ptr);
		break;
	case 1:
		fptr = max_arr;
		//max_arr(ptr);
		break;
	case 2:
		fptr = min_arr;
		//min_arr(ptr);
		break;
	case 3:
		fptr = pow;
		break;
	default:
		printf("잘못된 입력");
		exit(1);
		break;
	}
	fptr(ptr);
	


}

void add(const int* ptr[]) {
	int sum = 0;
	for (int i = 0; i < ROWS; i++) {
		for (int j = 0; j < COLS; j++) {
			sum += ptr[i][j];
		}
	}

	printf("배열의 합은 : %d\n", sum);
}

void max_arr(const int* ptr[]) {
	int max = INT_MIN;
	for (int i = 0; i < ROWS; i++) {
		for (int j = 0; j < COLS; j++) {
			if (max < ptr[i][j]) max = ptr[i][j];
		}
	}

	printf("배열의 최대값은 : %d\n", max);
}

void min_arr(const int* ptr[]) {
	int min = INT_MAX;
	for (int i = 0; i < ROWS; i++) {
		for (int j = 0; j < COLS; j++) {
			if (min > ptr[i][j]) min = ptr[i][j];
		}
	}

	printf("배열의 최소값은 : %d\n", min);
}

void pow(const int* ptr[]) {
	printf("배열의 제곱은 : \n");
	for (int i = 0; i < ROWS; i++) {
		for (int j = 0; j < COLS; j++) {
			//printf("%d ", pow(ptr[i], 2));
			printf("%d ",ptr[i][j]*ptr[i][j]);

		}
		printf("\n");
	}
}
```
