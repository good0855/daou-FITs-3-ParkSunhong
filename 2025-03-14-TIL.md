# 구조체

- 서로 다른 형태의 데이터를 여러 개 모아서 하나의 자료구조로 표현
- 구조체로 만들어진 자료는 연속된 메모리에 할당한다.
- 구조체에 데이터는 크기에 따라 연속적으로 저장

## 구조체 정의 및 변수 선언

```c

```

## 익명 구조체

- 구조체를 선언할 수 있는 방법 중 다음과 같이 `typedef` 키워드를 사용해서 할 수 있다.
- 이때, 2가지 방법이 있는데, 후자가 **익명 구조체(Anonumous Struct)**이다.

```c
typedef struct student3 {
	char name[12];
	int age;
	char phone[12];
} ST;

typedef struct {
	char name[12];
	int age;
	char phone[12];
} ST
```

### ✅ 첫 번째 방식: `typedef struct student3 { ... } ST;`

```c
typedef struct student3 {
    char name[12];
    int age;
    char phone[12];
} ST;

```

✔ **특징**

- `struct student3`라는 **구조체 태그**를 가짐.
- `ST`라는 **별칭**(타입명)도 가짐.
- 따라서 두 가지 방식으로 선언 가능:
    
    ```c
    struct student3 s1;  // struct를 사용한 선언
    ST s2;               // typedef를 사용한 선언
    
    ```
    

---

### ✅ 두 번째 방식: `typedef struct { ... } ST;` (익명 구조체)

```c
typedef struct {
    char name[12];
    int age;
    char phone[12];
} ST;

```

✔ **특징**

- 구조체에 **이름(struct 태그)이 없음** → **익명 구조체(Anonymous Struct)**.
- `ST`라는 **별칭(타입명)만 사용 가능**.
- `struct` 키워드를 사용할 수 없고, 오직 `ST`로만 선언 가능:
    
    ```c
    ST s1;  // ✅ 가능
    struct ??? s2;  // ❌ 불가능 (태그명이 없음)
    
    ```
    

---

### 📌 차이점 정리

| 방식 | 구조체 태그 | `typedef` 별칭 | 선언 방식 |
| --- | --- | --- | --- |
| `typedef struct student3 { ... } ST;` | `student3` 있음 | `ST` 있음 | `struct student3 s1;` 또는 `ST s2;` |
| `typedef struct { ... } ST;` | 없음 (익명 구조체) | `ST` 있음 | `ST s1;`만 가능 |

✔ **어떤 방식을 사용할까?**

- 구조체를 **포인터로 전달하는 경우**, 첫 번째 방식(`struct student3`)이 유리함.
    
    ```c
    void printStudent(struct student3 *s);  // struct 태그 사용 가능
    
    ```
    
- 그냥 간단히 **별칭만 필요하면** 두 번째 방식(`typedef struct {}`)이 더 직관적.

---

## 문자열 입력 함수 정리

공통으로 `"문자열 + Enter"`를 입력받았다고 가정한다.

---

## **1. scanf()**

`scanf`함수는 `Enter`나 `공백`전까지 문자열을 가져오고 마지막에 `널문자`를 붙인다. ⇒ 즉 공백 저장 안함

## **2. gets()**

`gets`함수는 문자열을 포함해서 `Enter`까지 가져오고, Enter를 `널문자`로 바꾸어준다. ⇒ Enter 키가 남아있는 buffer 안비워줘도 ㄱㅊ

## **3. fgets()**

`fgets`함수는 gets 함수와 마찬가지로 `Enter`까지 가져오지만, Enter를 그대로 두고 그 뒤에 추가로 `널문자`를 붙인다. ⇒ Enter 살림
