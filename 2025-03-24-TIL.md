# 2025-03-24-TIL

# 전처리기

## 1. `#include`

- 코드의 재사용과 유지보수성 향상
    - 코드의 모듈화
- 용도 : 반복되는 코드를 제거할 때 이용, 함수 및 구조체를 헤더 파일로 분리

## 2. `#define`

- 코드 간소화 및 가독성 향상할 때 이용
- 매크로 사용 ⇒ 상수 및 함수를 간결하게 정의 가능

## 3. `#ifdef`, `#ifdef`, `#if`

- 특정 조건에 따라 코드 실행을 다르게 설정할 수 있다.
- 디버킹 코드관리
- 운영체제 또는 환경에 따른 코드 분기 처리

```c
// myheader.h
void hello();

```

```c
// myheader.c
#include <stdio.h>

void hello() {
	printf("Hello World!\n");
}
```

## 4. `#pragma`

- 컴파일러 지시
- 특정 컴파일러 설정을 변경할 수 있다.

```c
 #pragma warning(disable: 4996) //C4996 경고 무시 (보안 경고 무시)
 #pragma warning(push)
 #pragma warning(pop)//일시적으로 경고비활성화 후 복원
 #pragma warning(default: 4996) //특정경고를 다시 복원

```

## 5. `#error`

- 특정 조건에서 컴파일 중단 ⇒ 오류 메시지 표시
- 잘못된 환경에서 빌드 막음
- 사용자가 특정 설정을 강제하도록 유도
- 

```c
#ifndef SUPPORTED_VERSION
	 #error "이 프로그램은 해당 환경에서 지원되지 않습니다!"
#endif
```

- 예제

```c
#include <stdio.h>
 // 특정 운영체제(OS) 체크
#ifndef _WIN32
 #error "이 프로그램은 Windows에서만 컴파일할 수 있습니다!"
#endif

 // C99 이상에서만 빌드 가능하도록 강제
#if __STDC_VERSION__ < 199901L
 #error "C99 이상이 필요합니다. 컴파일러를 업그레이드하세요!"
#endif

int main() {
 printf("Windows에서 C99 표준으로 컴파일되었습니다!\n");
 return 0;
}
```

```c
#include <stdio.h>
 // 특정 운영체제(OS) 체크
#ifndef _WIN32
 #error "이 프로그램은 Windows에서만 컴파일할 수 있습니다!"
#endif

 // C99 이상에서만 빌드 가능하도록 강제
#if __STDC_VERSION__ < 199901L
 #error "C99 이상이 필요합니다. 컴파일러를 업그레이드하세요!"
#endif

int main() {
 printf("Windows에서 C99 표준으로 컴파일되었습니다!\n");
 return 0;
}
```

<aside>
💡

`#pragma once` 와 `#ifndef` 차이?

</aside>

- 두 기능 모두 header 파일이 중복 포함되는 것을 방지함
- **`#pragma once`**
    - 각 파일별로 프리프로세서가 include 한 상태를 기억하면 되므로 한 번 읽은 헤더 파일은 읽지 않아도 된다.
        - 컴파일 속도 측면에서 보면 **#pragma once 가 더 유리**
    - 표준이 아니라는 문제가 존재(그런데 사실상 표준)
        - C++계열과 GCC까지 지원함

- **include guard**
    - 한 번 읽은 헤더 파일도 일단 헤더 파일의 내용을 다 읽어야 함
        
        C 언어로 `#include guards`와 `#pragma once`를 사용하는 예시를 보여드리겠습니다. 두 방법 모두 헤더 파일이 중복으로 포함되지 않도록 하는 방식인데, 컴파일러에서 처리하는 방식에 차이가 있습니다.
        
        ### 1. **Include Guards 예시 (C 언어)**:
        
        헤더 파일에 `#include guards`를 사용하여, 헤더 파일 내용이 중복으로 포함되지 않도록 조건문을 사용합니다.
        
        ```c
        // my_header.h
        #ifndef MY_HEADER_H  // MY_HEADER_H가 정의되어 있지 않으면 아래 내용 포함
        #define MY_HEADER_H
        
        // 헤더 파일 내용
        typedef struct {
            int value;
        } MyStruct;
        
        void myFunction();
        
        #endif  // MY_HEADER_H
        
        ```
        
        이 방식에서는 `MY_HEADER_H` 매크로가 한 번 정의되면, 동일한 파일이 다시 포함될 때 이 매크로가 이미 정의되어 있기 때문에 헤더 파일 내용이 중복으로 포함되지 않습니다. 하지만 매번 `#ifndef MY_HEADER_H`와 같은 조건을 확인하는 과정이 필요합니다.
        
        ### 2. **#pragma once 예시 (C 언어)**:
        
        `#pragma once`를 사용하면, 파일이 중복으로 포함되지 않도록 프리프로세서가 자동으로 처리합니다. `#pragma once`는 조건문 없이 파일의 포함 상태를 추적하여, 이미 포함된 파일은 다시 읽지 않도록 관리합니다.
        
        ```c
        // my_header.h
        #pragma once
        
        // 헤더 파일 내용
        typedef struct {
            int value;
        } MyStruct;
        
        void myFunction();
        
        ```
        
        `#pragma once`는 파일을 한 번 읽은 후, 해당 파일을 다시 읽지 않도록 해줍니다. 이 방식은 코드가 더 간결하고, 컴파일러가 파일을 추적하기 때문에 컴파일 속도 측면에서 효율적입니다.
        
        ### 차이점 요약:
        
        - **Include Guards**:
            - `#ifndef`, `#define`, `#endif`를 사용하여 헤더 파일의 중복 포함을 방지.
            - 매번 조건을 평가하는 과정이 필요해서 처리 시간이 더 걸릴 수 있습니다.
            - 조건문으로 매크로 정의를 검사합니다.
        - **#pragma once**:
            - 컴파일러가 해당 파일을 한 번만 포함하도록 처리.
            - 코드가 간결하고, 매크로 정의 검사 없이 한 번만 읽습니다.
            - 컴파일 속도가 더 빨라질 수 있습니다.
    - 각 헤드 파일에 대응하는 매크로 이름이 서로 충돌할 가능성이 존재
    - 따라서 include guards 명명 규칙을 정해서 충돌 가능성을 줄여야 한다.
        - ex) _MODULE_HEADER_H 를 통해 모듈명, 헤더명 이렇게
    - 
- 참고 : https://yesarang.tistory.com/67

---

## 실습문제

- 운영체제별 문제

```c
#include <stdio.h>

// 운영체제 매크로 정의
#ifdef _WIN32
    #define OS_WINDOWS
#elif defined(__linux__)
    #define OS_LINUX
#elif defined(__APPLE__)
    #define OS_MAC
#else
    #error "지원되지 않는 운영체제입니다."
#endif

void preprocessor() {
    #ifdef OS_WINDOWS
        system("cls");
        printf("윈도우 환경입니다.\n");
    #elif defined(OS_LINUX) || defined(OS_MAC)
        system("clear");
        printf("LINUX or MAC 환경입니다.\n");
    #endif
}

int main() {
    preprocessor(); 
    return 0;
}

```

[문제4] 특정 환경에서만 최대 거래한도를 다르게 설정하는 프로그램을 작성하세요.
[요구사항]- 일반 사용자(USER_NORMAL)은 최대 거래한도가 1,000,000원입니다.- VIP 사용자(USER_VIP)는 최대 거래 한도가 10,000,000원입니다- 환경이 정의 되지 않으면 기본 한도는 5,000,000원으로 설정됩니다- #define USER_NORMAL 또는 #define USER_VIP를 이용해서 사용자의 등급을 변경할 수
있습니다

```c
#include <stdio.h>

#define USER_NORMAL 
#define USER_VIP

int main()
{
    int limit = 5000000;
    
    #ifdef USER_NORMAL
        limit = 1000000;
    #elif defined(USER_VIP)
        limit = 10000000;
    #endif
    
    printf("limit : %d", limit);

    return 0;
}

```

---

# 2. 모듈화

- 모둘화는 프로그램을 독립적인 기능을 수행하는 작은 단위(모듈)로 나누어 개발하는 소프트웨어 설계 기법이다.
- 모듈은 각각의 기능을 담당하며 서로 독립적으로 작성,수정,테스트할 수 있어 유지보수성과 재사용성이 증가한다.
- 논리적으로 같은 등급의 소스 파일을 계속해서 분할하는 것이다.- 소스파일에서 혹은 어떤 함수에서 일어나는 일의 책임을 그 소스 파일과 그 함수가 지도록 하는 것이다
1. **헤더파일과 소스파일을 분리하여 모듈화를 구현한다.**
헤더파일(.h) : 함수 선언 및 전역 변수 정의하는 부분
소스파일(.c) : 함수 실제 구현하는 부분
함수선언(인터페이스)와 구현을 나누어 관리하는 것이다.
2. 독립적인 코드 관리 기능
필요할 때 필요한 모듈만 수정하면 된다.
한 모듈을 다른 프로젝트에서도 재사용할 수 있다
3. 컴파일 시간 단축 및 협업이 용이하다
여러 개발자가 각각이 모듈을 동시에 개발 가능하다
변경된 파일만 다시 컴파일하면 되므로 빌드 속도를 개선한다.

## 모듈화 방법

1. 헤더 파일(.h)과 소스 파일(.c) 분리하기
헤더 파일 – 함수 선언, 매크로, typedef
소스 파일 – 함수 정의 및 내부 로직 구현
2. 각 모듈마다 역할을 명확히 분리하기
main.c – 프로그램 진입점 역할
bank.h, bank.c – 시스템의 핵심 로직 역할
db.h, db.c – 데이터베이스 연동 역할
ui.h, ui.c – 화면에 관련된 역할
3. 헤더 가드 사용하기
#ifndef ~ #define ~ #endif
예)
#ifndef BANK_H
#define BANK_H
void deposit(int amount );
void withdraw(int amount );
#endif
4. 전역 변수는 extern으로 선언하여 여러 파일에서 공유하기
예) bank.h
extern int balance;
    
    bank.c
    int balance = 1000;
    
5. static을 사용하여 **내부에서만 사용되는 함수 숨기기**
외부에서 접근 불가능한 변수를 사용해서 독립적으로 모듈화할 수 있다.
예) static void interanl_function() {
//외부에서 접근 불가
}
