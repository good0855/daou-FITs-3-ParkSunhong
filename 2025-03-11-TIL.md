# C언어 변수 종류

<aside>
💡

가능하면 함수 매개변수나 정적변수를 이용

</aside>

## 1. 지역변수 (Local Variable)

- 함수 내부 또는 중괄호 `{}` 내부에서 선언되는 변수이다.
- **지역**이란 변수의 생존 범위를 의미하며, 함수 내부나 특정 코드 블록 내부를 뜻한다.
- 변수가 선언된 블록을 벗어나면 **메모리 공간이 소멸**된다.
- **초기화하지 않으면** 컴파일 오류가 발생하거나 쓰레기 값이 저장될 수 있다.

- 아래 예시에서는 `count` 라는 변수가 매 번 초기화된다.

```c
#include <stdio.h>

void countFunction();
int main() {

	countFunction();
	countFunction();
	countFunction();
	countFunction();
	return 0;
}

void countFunction() {
	int count = 0;
	count++;
	printf("%d\n", count);
}
```

---

## 2. 전역변수 (Global Variable)

- 중괄호 `{}` 외부에서 선언되는 변수이다.
- 프로그램의 **어느 지역에서든 참조 가능**하다.
- **전처리기 아래에 선언되며, 반드시 상수로 초기화해야 한다.**
- 초기값을 지정하지 않으면 **기본값 0으로 자동 초기화**된다.
- 프로그램 종료 전까지 메모리가 유지된다.
- **잘못 사용하면 유지보수와 재사용성이 어려워질 수 있다.**
- 이러한 단점을 보완하기 위해 **정적변수(static variable)**가 등장했다.

### 2.1 외부 변수(External Variable)

- 전역 변수중 하나로 여러 파일에서 공유할 수 있는 변수이다.
- `extern` 키워드를 사용하여 다른 파일에서 선언된 변수를 참조할 수 있다.
    - 다른 파일에서는 `extern`을 통해 참조만 할 수 있다
- 초기화된 변수는 선언된 파일에서만 메모리를 차지한다.

```c
// file3.c
#include <stdio.h>

int globalVar = 10;  // 외부 변수 (전역 변수)

void printGlobalVar() {
	printf("globalVar in file1.c: %d\n", globalVar);
}
```

```c
// file4.c
#include <stdio.h>

extern int globalVar;  // file3.c에 있는 외부 변수 선언 (실제 메모리 할당 X)

void modifyGlobalVar() {
	globalVar += 5;  // 값 변경
	printf("globalVar in file2.c after modification: %d\n", globalVar);
}
```

```c
#include <stdio.h>

void printGlobalVar();
void modifyGlobalVar();

 int main() {
			printGlobalVar();      
			modifyGlobalVar();     
			printGlobalVar();      
		return 0;
 }
```

- 이용분야
    - 파일 간 공유 가능
    - 메모리를 한 번만 할당한다
    - 어디서든지 수정 가능하다. 하지만 너무 남발하면 유지보수가 어렵다
    - 남발하면 코드의 모듈성이 떨어지고 디버깅이 어려워진다.

---

## 3. 정적변수 (Static Variable)

- 프로그램 종료 전까지 **메모리가 유지되는 변수**이다.
- 함수나 코드 블록을 벗어나더라도 **값이 유지**된다.
- **초기화할 때 반드시 상수로 초기화해야 한다.**
- 초기화하지 않으면 **기본값 0으로 자동 초기화**된다.

### 3.1 정적변수의 종류

### 1) 정적 전역변수 (Static Global Variable)

- **자신이 선언된 소스 파일에서만 접근 가능**하다.
- 즉, 전역변수 앞에 `static`을 붙이면 **변수의 범위를 해당 파일 내로 제한**하는 효과가 있다.
- 외부에서 참조할 수 없기 때문에, **캡슐화 효과**를 얻을 수 있다.
    - `extern` 키워드로 다른 파일에서 가져올 수 없음

### 2) 정적 지역변수 (Static Local Variable)

- **중괄호 `{}` 내부에서만 사용 가능**하다.
- 단, **한 번 초기화되면 함수가 여러 번 호출되어도 값이 유지된다.**
- 일반 지역변수와 달리 **메모리가 소멸되지 않고 계속 유지**된다.

```c
#include <stdio.h>

void countFunction();
int main() {

	countFunction();
	countFunction();
	countFunction();
	countFunction();
	return 0;
}

void countFunction() {
	static int count = 0;
	count++;
	printf("%d\n", count);
}
```

---

## 4. 레지스터 변수

- CPU 레지스터에 저장될 가능성이 있는 변수
- 목적은 자주 사용하는 변수를 메모리가 아니라 레지스터에 저장하여 접근 속도를 높이는 것
이다
- 컴파일러가 register라고 쓰면 반드시 레지스터에 저장하는 것은 아니다. 단순한 제안일 뿐
이다.
- 포인터로 접근할 수 없다(& 연산자 사용할 수 없다)
- 현재 컴파일러는 최적화를 자동으로 수행하므로 register 키워드는 거의 사용하지 않는다

---

# 포인터

## 컴퓨터 작동 원리

1. 기계어로 번역되어 실행파일(exe)로 저장된다.
2. 로더에 의해 실행 파일이 메모리에 적재되면서 실행
3. 모든 변수는 컴파일러에 의해 생성되는 symbol table에 저장되며, 해당 테이블에서는 변수명과 메모리 주소(값이 저장된 주소)가 저장
4. 프로그램이 실행될 때 모든 변수는 심벌 테이블에 있는 주소에 의해 접근!

## 연산자

`&` : 주소 연산자

`*` : 간접 참조 연산자, 포인터 변수에 사용

- 포인터 변수는 주소를 저장하는 변수이다.
    - 일반 변수의 주소, 배열 주소, 함수 주소 등도 포인터 변수에 배정 가능함
    - 포인터 변수 선언 : `자료형 *변수이름;`

```c
int max;
int *pmax; // 포인터를 저장하는 변수에 사용
a = 100;

pmax = &a; // 저장
printf("%d", *pmax); // 포인터 변수가 가리키는 변수값을 의미
printf("%p", pmax); // 포인터 주소 출력

```

- 심볼 테이블 ; https://ko.ittrip.xyz/c/c-symbol-table-linker

## 포인터와 배열의 관계

- 배열의 이름 : 주소상수와 같은 역할
- 배열의 이름 : 배열 첫번째 요소의 주소 상수값을 가짐
- 배열의 이름은 포인터 변수와 같은 역할을 하므로 포인터 변수 대신 직접 사용 가능
- 포인터 변수를 선언 ⇒ 배열의 이름으로 초기화를 수행하여 사용하는 것도 가능
- 두 방법의 차이
    - 배열의 이름을 나타내는 변수 = 기억 장소에 저장되지 않음
    - 별도의 포인터 변수를 선언하여 사용 = 포인터 변수 자체가 메모리에 저장

## 다중 포인터

- 배열과 문자열의 조작에 포인터를 사용함으로써 간결하고 효율적인 처리
- 배열, 구조체, 공용체와 함수에 포인터를 사용하여 쉽게 접근하기 위해
- 배열의 첨자보다 포인터를 사용하는 것이 더 빨리 처리 가능

## 실습 22

```c
#include <stdio.h>

int main() {

	// 실습과제 22번
	double d = 100.0;
	double* dpoint = &d;
	printf("변수 d의 값: %f\n", d); 
	printf("변수 d의 주소 값: %p\n", &d);
	printf("dpoint의 값: %p\n", dpoint);
	printf("*dpoint(포인터 변수)가 가리키는 값: %f\n", *dpoint);
	printf("변수 d의 크기: %d\n", sizeof(d));
	printf("변수 d의 주소의 크기: %d\n", sizeof(&d));
	printf("dpoint(포인터 변수) 크기: %d\n", sizeof(dpoint));
	printf("*dpoint(포인터 변수가 가리키는 변수) 크기: %d\n", sizeof(*dpoint));

	return 0;
	
}
```

## 실습 23

```c
#include <stdio.h>

int main()
{
    int i, *ip = &i;
    
    int sum = 0, *sump = &sum;
    
    for(*ip = 1; *ip <= 100; (*ip)++) {
        *sump += *ip;
    }
    
    printf("포인터 변수를 사용한 1~100까지의 합은: %d\n", sum);

    return 0;
}
```

## 실습 24

```c
#include <stdio.h>

int main()
{
    
    int score = 100;
    int *pscore, **ppscore, ***pppscore;
    pscore = &score;
    ppscore = &pscore;
    pppscore = &ppscore;
    
    printf("포인터 변수 *pscore의 값은 %d\n", *pscore);
    printf("포인터 변수 **ppscore의 값은 %d\n", **ppscore);
    printf("포인터 변수 ***pppscore의 값은 %d\n", ***pppscore);
    printf("================================\n");
    
    printf("int변수 score의 주소는 %p\n", &score);
    printf("포인터 변수 pscore의 값(주소)은 %p\n", pscore);
    printf("================================\n");
    
    printf("포인터 변수 pscore의 주소는 %p\n", &pscore);
    printf("포인터 변수 ppscore의 값(주소)은 %p\n", ppscore);
    printf("================================\n");
    
    printf("포인터 변수 ppscore의 주소는 %p\n", &ppscore);
    printf("포인터 변수 pppscore의 값(주소)은 %p\n", pppscore);
    return 0;
}
```

## 실습 25

```c
#include <stdio.h>

int main()
{
    int score[6] = {99, 89, 98, 88, 85};
    int *pscore, i, sum = 0;
    pscore = score;
    
    if(pscore+2 == score+2 && pscore+2 == &score[2]) {
        printf("두 번째 요소의 주소가 모두 같다.\n");
    }
    
    if(*(pscore+2) == *(score+2) && *(pscore+2) == &score[2]) {
        printf("두 번째 요소의 값도도 모두 같다.\n");
    }
    
    printf("포인터 변수 pscore의 값(배열 주소) : %p\n", pscore);
    printf("배열 이름 score의 값(배열 주소) : %p\n", score);
    
        printf("포인터 변수 pscore의 값(배열 주소)의 주소소 : %p\n", &pscore);
    printf("배열 이름 score의 값(배열 주소)의 주소소 : %p\n", &score);
    
    sum = 0;
    
    for(int i = 0; i < 5; i++) {
        sum = sum += *pscore++;
    }
    
    printf("포인터 변수를 이용한 합계는 %d\n", sum);
    
}
```
