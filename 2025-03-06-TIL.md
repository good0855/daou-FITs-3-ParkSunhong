# 2025-03-06-TIL

`scanf` 사용 시 주의사항

1. 반환 명세 안에 반환명세가 아닌 다른 문자를 넣지 않는다
2. “”로 닫기 전에 공백, 개행 X
- 실습
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    
    int main() {
    
    	char gender;
    	int age;
    	double height;
    
    	printf("성별은?(남자 : M / 여자 : F) >> ");
    	scanf("%c", &gender);
    	getchar(); // 엔터 값도 흡수
    
    	printf("나이는? >> ");
    	scanf("%d", &age);
    	getchar();
    
    	printf("키는? >>");
    	scanf("%lf", &height);
    	getchar();
    
    	printf("================\n");
    	printf("성별 : %c\n", gender);
    	printf("나이 : %3d\n", age);
    	printf("키 : %.lf\n", height);
    
    	return 0;
    }
    ```
    

## 문자열

- C언어에서는 별도의 문자열 클래스가 존재하지 않음
    - 문자열은 배열로 표현
    - ex = “a\0” (\0)은 끝을 나타내는 null 문자열
    - `char name[10]`

- 문자열 입출력은 다음 함수로 입력받음
    - `gets(name)`
    - `puts(name)`

- 배열명은 배열의 시작 주소(첫번째를 가리킴)
    - `name`은 `&name[0]` 과 동일

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {

	char name[10];

	printf("이름은? : ");
	gets(name);
	puts(name);

	return 0;
}
```

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {

    char name[10];

    printf("이름은? : ");
    scanf_s("%s", name, 10); // name 배열의 변수명은 배열의 첫 번째를 가리킴
    printf("%s\n", name);

    // 입력 버퍼 비우기
    getchar();

    char gender;
    printf("성별은 m, f >>");
    gender = getchar();
    putchar(gender);

    return 0;
}

```

# 실습 6

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {

    int a, b, c;
    printf("값 3개 동시 입력 : ");
    scanf_s("%d %d %d", &a, &b, &c);
    getchar();

    printf("출력: %d, %d, %d\n", a, b, c);
}

```

## 실습 7

```c
#include <stdio.h>

int main()
{
    int age;
    char gender;
    double height;
    
    // 자료 입력 받기
    printf("성별은? (남자라면 M, 여자라면 F) \n");
    scanf(" %c", &gender); // 공백 추가하여 개행 문자 무시
    printf("나이는? \n");
    scanf("%d", &age); // 정수 입력
    printf("키는? \n");
    scanf("%lf", &height); // 실수 입력
    
    printf("\n=====================\n");
    printf("성별: %c\n", gender);
    printf("나이: %d\n", age);
    printf("키: %.2lf\n", height); // 키는 소수점 둘째 자리까지 출력
    
    return 0;
}

```

---

# 연산자

1. 산술연산자
2. 대입연산자
3. 논리연산자
4. 비교연산자
5. 증감연산자
6. 비트연산자
- C언어에서는 하드웨어의 제어에 사용된다
- 종류 : 비트논리연산자, 시프트연산자
1. 콤마연산자
2. 삼항연산자
3. 형변환연산자
4. 크기연산자

