# 2025-03-07-TIL
# 함수와 구조체

## 구조체 초기화

- 다음과 같은 구조체가 있다면

```c
typedef struct {
	int account_id;
	int balance;
} Account;

typedef struct {
	char name[30];
	int age;
	int id;
	int account_size;
	Account accounts[10];
} Customer;
```

1. 기본 초기화
    - `accounts[10]`을 **빈 배열 `{}`로 초기화**
    - `accounts` 배열의 모든 요소는 `0`으로 자동 초기화됨
    
    ```c
    Customer customer = { "홍길동", 20, 1000, {} };
    ```
    
2. 일부만 초기화
    - `accounts[0] = {101, 5000}`
    - `accounts[1] = {102, 10000}`
    - 나머지 `accounts[2] ~ accounts[9]`는 `{0, 0}`으로 자동 초기화됨
    
    ```c
    Customer customer = { "홍길동", 20, 1000, { {101, 5000}, {102, 10000} } };
    
    ```
    

1. 재할당
    - 선언과 동시에 초기화를 하는게 아니라 다시 재할당을 한다면 다음과 같이 해줘야 한다.
    
    ```c
    customer.accounts[i] = (Account){ 0 };
    
    ```
    

## 실습 30

```c
#include <stdio.h>

typedef struct student{
	char name[20];
	char sex;
	int stid;
	int sub1;
	int sub2;
	int sub3;
	double avg;
} ST;

int input(ST st);
int main() {
	ST st1 = { "kdhong", 'm', 1508001, 0, 0, 0, 0 };
	input(st1);
	printf("main 함수에서 세 과목의 점수는 %d %d %d입니다.", st1.sub1, st1.sub2, st1.sub3);
	return 0;
}

int input(ST st) {
	printf("%s 학생의 3과목 성적을 입력하세요: ", st.name);
	scanf_s("%d %d %d", &st.sub1, &st.sub2, &st.sub3);
	printf("input 함수에서 입력한 값은 %d, %d, %d\n", st.sub1, st.sub2, st.sub3);
}
```

- 구조체 인자(Call by value)

코드를 실행하면 `input` 함수에서 입력한 값과 `main` 함수에서 출력한 값이 다르게 나타납니다. 그 이유는 **C 언어에서 함수 인자로 구조체를 전달할 때 값 호출(call by value)이 기본이기 때문**입니다.

### 🔹 **발생하는 문제**

```c
int input(ST st);

```

위 함수 선언을 보면 `input` 함수는 `ST` 구조체를 **값으로(pass by value)** 받습니다. 즉, `input(st1);`을 호출할 때 `st1`의 복사본이 함수로 전달됩니다.

```c
int input(ST st) {
    printf("%s 학생의 3과목 성적을 입력하세요: ", st.name);
    scanf_s("%d %d %d", &st.sub1, &st.sub2, &st.sub3);
    printf("input 함수에서 입력한 값은 %d, %d, %d\n", st.sub1, st.sub2, st.sub3);
}

```

- `st`는 `st1`의 복사본이므로, `scanf_s`로 입력받아도 `main` 함수의 `st1`에는 변경 사항이 반영되지 않습니다.
- 즉, `input` 함수에서 `sub1`, `sub2`, `sub3` 값을 변경해도 이는 복사본에 적용될 뿐, `main`의 `st1`에는 영향을 미치지 않습니다.

따라서 `input` 함수에서 출력한 점수와 `main` 함수에서 출력한 점수가 다르게 나오는 것입니다.

---

### ✅ **해결 방법**

1. **포인터를 사용하여 참조 전달(call by reference)**

```c
int input(ST *st);  // 포인터 매개변수로 변경

int main() {
    ST st1 = { "kdhong", 'm', 1508001, 0, 0, 0, 0 };
    input(&st1);  // 주소를 전달
    printf("main 함수에서 세 과목의 점수는 %d %d %d입니다.\n", st1.sub1, st1.sub2, st1.sub3);
    return 0;
}

int input(ST *st) {  // 포인터를 사용하여 원본 수정
    printf("%s 학생의 3과목 성적을 입력하세요: ", st->name);
    scanf_s("%d %d %d", &st->sub1, &st->sub2, &st->sub3);
    printf("input 함수에서 입력한 값은 %d, %d, %d\n", st->sub1, st->sub2, st->sub3);
}

```

✔️ `input(&st1);` → `st1`의 주소를 전달

✔️ `input` 함수에서 `->` 연산자로 구조체 멤버에 접근하여 원본 값 수정

이렇게 하면 `input` 함수에서 입력한 값이 `main`에서도 동일하게 반영됩니다.

---

### **🔎 결론**

C 언어에서 구조체를 함수 인자로 전달할 때 값 호출(call by value) 방식이 기본이므로, 함수 내부에서 구조체를 수정해도 원본에는 영향을 주지 않습니다.

✅ **구조체를 수정해야 한다면 포인터를 사용하여 참조 전달(call by reference)을 해야 합니다.**

## 실습 31

```c
#include <stdio.h>

typedef struct student{
	char name[20];
	char sex;
	int stid;
	int sub1;
	int sub2;
	int sub3;
	double avg;
} ST;

int input(ST *st);
int main() {
	ST st[3] = {
		{"kdhong", 'm', 1508001, 0, 0, 0, 0},
		{"yhkim", 'w', 1508002, 0, 0, 0, 0},
		{"cskim", 'm', 1508003, 0, 0, 0, 0},

	};

	for (int i = 0; i < 3; i++) {
		input(&st[i]);
		printf("main 함수에서 세 과목의 점수는 %d %d %d입니다.\n", st[i].sub1, st[i].sub2, st[i].sub3);  // 수정됨

	}

	return 0;
}

int input(ST *st) {
	printf("%s 학생 성적 입력 3과목 성적을 입력하세요: ", st->name);
	scanf_s("%d %d %d", &st->sub1, &st->sub2, &st->sub3);
	printf("input 함수에서 입력한 값은 %d, %d, %d\n", st->sub1, st->sub2, st->sub3);
}
```

---

## 문제

구조체와 표인터 응용문제 1,2,3, 4

[10-1 구조체와 포인터 응용문제.pdf](attachment:abd838f0-dc66-4c83-92d8-041998f009b0:10-1_구조체와_포인터_응용문제.pdf)

```c
#include <stdio.h>
#include <time.h>
#include <math.h>

#define MAX_DEPOSITS 5
#define STOCKS 3

typedef long long ll;
typedef struct {
	int account_id;
	int balance;
} Account;

typedef struct {
	char name[30];
	int age;
	int id;
	int account_size;
	Account accounts[10];
} Customer;

typedef struct {
	char name[30];
	double investment_amount;
	double return_rate;
} Investment;

typedef struct {
	char name[30];
	int num_investments;
	Investment investments[10];

} Customer_2;

typedef struct FixedDeposit {
	char name[30];
	char account_number[10];
	struct tm start_date;
	int duration;
	double balance;
} FixedDeposit;

typedef struct Stock {
	char name[20];
	char ticker[6];
	int count;
	double low_prices[30];
	double avg_price;
	double std_price;
} Stock;

void f_001();
void f_002();
void add_investment(Customer_2* customer, char* name, double investment_amount, double return_rate);
void f_003();
void display_deposits(const FixedDeposit* deposits);
void sort_by_maturity_date(FixedDeposit* deposits);

// 4번
void f_004();
void calculate_average(Stock* stocks);
void calculate_volatility(Stock* stocks);
char* categorize_risk(const Stock* stock);
void display_portfolio(const Stock* stocks);

int main() {
	//f_001();
	//f_002();
	//f_003();
	f_004();
}
void f_001() {
	Customer customer = { "홍길동", 20, 1000, 0, {0} };
	while (1) {
		int cmd;
		printf("\n1. 계좌 추가\n");
		printf("2. 계좌 삭제\n");
		printf("3. 고객 정보 보기\n");
		printf("4. 종료\n");
		printf("원하는 작업을 선택하세요: ");
		scanf_s("%d", &cmd);

		switch (cmd) {
		case 1:
			int account_id;
			int account_balance;

			printf("추가할 계좌번호를 입력하세요: ");
			scanf_s("%d", &account_id);

			printf("초기잔액을 입력하세요: ");
			scanf_s("%d", &account_balance);

			customer.accounts[customer.account_size].account_id = account_id;
			customer.accounts[customer.account_size].balance = account_balance;

			customer.account_size++;
			break;
		case 2:
			int del_account_id;
			printf("삭제할 계좌번호를 입력하세요: ");
			scanf_s("%d", &del_account_id);

			for (int i = 0; i < customer.account_size; i++) {
				if (customer.accounts[i].account_id == del_account_id) {
					for (int j = i; j < customer.account_size - 1; j++) {
						customer.accounts[j].account_id = customer.accounts[j + 1].account_id;
					}
					customer.account_size--;
					break;
				}

			}
			break;
		case 3:
			int num_accounts = customer.account_size;
			printf("고객 : %s\n", customer.name);
			printf("계좌 수 : %d\n", num_accounts);

			for (int i = 0; i < num_accounts; i++) {
				printf("계좌 %d : 잔액 %d\n",
					customer.accounts[i].account_id,
					customer.accounts[i].balance);

			}

			break;
		case 4:
			printf("프로그램을 종료합니다.");
			exit(0);
			break;
		default:
			printf("다시 입력하세요\n");
		}
	}
}
void f_002() {
	char name[30];
	int num;
	Customer_2 customer = { "", 0, {0} };

	printf("고객 이름은? : ");
	scanf_s("%s", &name, 30);

	printf("투자 상품 개수는? : ");
	scanf_s("%d", &num);

	for (int i = 0; i < num; i++) {
		char name[30];
		double investment_amount;
		double return_rate;

		printf("투자 상품 %d를 입력하세요(회사명, 투자금액, 수익률) : ", i+1);

		scanf_s("%s%lf%lf", &name, 30, &investment_amount, &return_rate);

		add_investment(&customer, name, investment_amount, return_rate);
	}

	printf("각 투자 상품의 수익\n");

	for (int i = 0; i < customer.num_investments; i++) {
		Investment* inv = &customer.investments[i];
		printf("%s 투자 상품의 수익: %.2lf\n", inv->name, inv->investment_amount * inv->return_rate / 100);

	}
	
}
void add_investment(Customer_2* customer, char* name, double investment_amount, double return_rate) {
	if (customer->num_investments >= 10) {
		printf("더 이상 투자를 추가할 수 없습니다.\n");
		return;
	}

	Investment* inv = &customer->investments[customer->num_investments]; // 포인터 사용
	strncpy(inv->name, name, sizeof(inv->name) - 1);
	inv->name[sizeof(inv->name) - 1] = '\0'; // 문자열 종료 보장

	inv->investment_amount = investment_amount;
	inv->return_rate = return_rate;

	customer->num_investments++; // 투자 개수 증가
}

// 3번
void f_003() {
	FixedDeposit deposits[MAX_DEPOSITS] = {
	   {"김철수", "1234567890", {0, 0, 0, 10, 0, 124}, 12, 5000000},
	   {"이영희", "9876543210", {0, 0, 0, 1, 6, 123}, 24, 3000000},
	   {"박민수", "5678901234", {0, 0, 0, 15, 2, 123}, 18, 2000000},
	   {"조수빈", "3456789012", {0, 0, 0, 5, 8, 123}, 6, 1000000},
	   {"정하나", "6789012345", {0, 0, 0, 20, 10, 123}, 12, 7000000}
	};

	sort_by_maturity_date(deposits);

	display_deposits(deposits);
	

}
void display_deposits(const FixedDeposit* deposits) {
	// 현재시간
	time_t t = time(NULL);
	struct tm now_date = *localtime(&t);
	
	for (int i = 0; i < MAX_DEPOSITS; i++) {
		FixedDeposit* now_deposit = deposits + i;

		// 만기일 설정(duration더하는 방식_
		struct tm end_date = now_deposit->start_date;
		end_date.tm_mon += now_deposit->duration;
		mktime(&end_date);

		printf("%d.%s | 계좌 : %s | 예금 시작: %d-%02d-%02d | 만기: %d-%02d-%02d | %s\n",
			i + 1,
			now_deposit->name,
			now_deposit->account_number,
			now_deposit->start_date.tm_year + 1900,
			now_deposit->start_date.tm_mon + 1,
			now_deposit->start_date.tm_mday,
			end_date.tm_year + 1900,
			end_date.tm_mon + 1,
			end_date.tm_mday,
			difftime(mktime(&end_date), t) <= 0 ? "만기 도래" : "만기 대기"
			);

	}
}
void sort_by_maturity_date(FixedDeposit* deposits) {
	for (int i = 0; i < MAX_DEPOSITS - 1; i++) {
		int min_idx = i;
		time_t min_time;

		struct tm min_end_date = deposits[i].start_date;
		min_end_date.tm_mon += deposits[i].duration;
		min_time = mktime(&min_end_date);

		for (int j = i + 1; j < MAX_DEPOSITS; j++) {
			struct tm temp_min_end_date = deposits[j].start_date;
			temp_min_end_date.tm_mon += deposits[j].duration;
			time_t temp_min_time = mktime(&temp_min_end_date);

			// 최솟값 갱신
			if (temp_min_time < min_time) {
				min_idx = j;
				min_time = temp_min_time;
			}
		}

		// swap
		if (min_idx != i) {
			FixedDeposit temp = deposits[i];
			deposits[i] = deposits[min_idx];
			deposits[min_idx] = temp;
		}

	}
}

// 4번
void f_004() {
	Stock stocks[STOCKS] = {
			 {"삼성전자", "005930", 100, {65400, 65200, 65000, 64800, 65500, 66000, 66200, 66100,
			66300, 65900, 65550, 65800, 66050, 66350, 66700, 66800, 67000, 66900, 67200, 67300,
			67100, 67050, 67250, 67400, 67600, 67500, 67450, 67650, 67800, 67900}},
			 {"테슬라", "TSLA", 50, {210, 215, 220, 218, 222, 225, 230, 228, 232, 235, 238, 240,
			245, 250, 248, 246, 244, 241, 238, 235, 233, 231, 228, 226, 224, 223, 220, 218, 215, 212}},
			 {"애플", "AAPL", 80, {180, 182, 184, 183, 185, 186, 188, 187, 189, 191, 193, 195, 198,
			200, 202, 203, 205, 207, 209, 210, 212, 213, 214, 215, 217, 218, 219, 220, 221, 222}}
	};

	// 평균 계산
	calculate_average(stocks);

	// 변동성 계산
	calculate_volatility(stocks);

	

	display_portfolio(stocks);
}
void calculate_average(Stock* stocks) {
	
	for (int i = 0; i < STOCKS; i++) {
		double sum = 0.0;
		double avg = 0.0;
		Stock* stock = (stocks + i);

		// 각 주식의 평균 계산
		int size = sizeof(stock->low_prices) / sizeof(stock->low_prices[0]);
		double *low_prices = stock->low_prices;
		for (int i = 0; i < size; i++) {
			sum += *(low_prices + i);
		}
		avg = sum / size;
		stock->avg_price = avg;
	}
	
}
void calculate_volatility(Stock* stocks) {
	for (int i = 0; i < STOCKS; i++) {
		double sum = 0.0;
		double std;
		Stock* stock = (stocks + i);

		// 각 주식의 평균 계산
		int size = sizeof(stock->low_prices) / sizeof(stock->low_prices[0]);
		double* low_prices = stock->low_prices;
		for (int i = 0; i < size; i++) {
			sum += pow( (*(low_prices + i) - stock->avg_price), 2);
		}
		std = sqrt(sum / (size));
		stock->std_price = std;;
	}

}
char* categorize_risk(const Stock* stock) {
	double std_price = stock->std_price;
	if (std_price >= 10.0) {
		return "높음";
	}
	else if (std_price >= 5.0) {
		return "보통";
	}
	else {
		return "낮음";
	}

}
	// 주식!
void display_portfolio(Stock *stocks) {
	printf("[고객 주식 포트폴리오 위험 분석]\n");
	for (int i = 0; i < STOCKS; i++) {
		Stock *stock = &stocks[i];
		printf("종목 : %s | 보유 주식 : %d | 변동성 : %.2lf | 위험 수준 : %s\n",
			stock->name,
			stock->count,
			stock->std_price,
			categorize_risk(stock)
		);
	}
}

	

```

---
