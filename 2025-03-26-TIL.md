# 2025-03-26-TIL
- 로그인 로직

### 수정된 `login` 함수:

```c
void login() {
    LoginInfo login_info;
    char query_select[512];
    
    printf("로그인을 진행합니다.\n");

    // 사용자 입력 받기
    printf("아이디 입력: ");
    scanf_s("%s", login_info.customer_id, (unsigned int)sizeof(login_info.customer_id));

    printf("비밀번호 입력: ");
    scanf_s("%s", login_info.customer_pw, (unsigned int)sizeof(login_info.customer_pw));

    // 고객 UUID 가져오기 위한 SELECT 쿼리 준비
    snprintf(query_select, sizeof(query_select),
        "SELECT customer_uuid, customer_pw FROM customers WHERE customer_id = '%s'",
        login_info.customer_id);

    // 결과를 저장할 구조체
    CustomerRow row = { 0 };

    FieldMapping mappings[] = {
        { offsetof(CustomerRow, customer_uuid), TYPE_INT, 0 },
        { offsetof(CustomerRow, customer_pw), TYPE_STR, CUSTOMER_MAX_PW_LENGTH }
    };
    int num_fields = sizeof(mappings) / sizeof(FieldMapping);

    // SELECT 쿼리 실행
    sword ret = executeSelectWithMapping(envhp, svchp, errhp, query_select, &row, mappings, num_fields);

    // 결과 확인
    if (ret == OCI_SUCCESS || ret == OCI_SUCCESS_WITH_INFO) {
        // 비밀번호 비교
        if (strcmp(login_info.customer_pw, row.customer_pw) == 0) {
            printf("로그인 성공!\n");
            current_user_uuid = row.customer_uuid;  // 로그인한 사용자 UUID 저장
        }
        else {
            printf("비밀번호가 일치하지 않습니다. 다시 시도하십시오.\n");
        }
    } else {
        // 고객 ID가 존재하지 않으면 로그인 실패
        printf("아이디가 존재하지 않습니다. 로그인 실패.\n");
    }
}
```

### 주요 변경사항:
1. **`customer_id` 입력**: 사용자로부터 `customer_id`와 `customer_pw`를 입력받습니다.
2. **쿼리 수정**: `customer_id`를 이용해 `SELECT` 쿼리를 실행하여 해당 고객의 `customer_uuid`와 `customer_pw`를 조회합니다.
3. **로그인 실패 시 메시지**: 
   - 사용자 정보가 없으면 "아이디가 존재하지 않습니다. 로그인 실패"를 출력하고 종료됩니다.
   - 비밀번호가 맞으면 "로그인 성공"을 출력하고, 로그인한 사용자의 `UUID`를 `current_user_uuid`에 저장합니다.
4. **로그인 성공 시**: 로그인 성공 시 `customer_uuid`가 저장됩니다.

이제 사용자가 `customer_id`와 `customer_pw`를 입력한 후, 데이터베이스에서 해당 사용자 정보를 조회하여 로그인 성공 여부를 출력하는 로직이 완성되었습니다.
