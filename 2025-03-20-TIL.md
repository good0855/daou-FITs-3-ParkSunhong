# 2025-03-20-TIL
---

## 1. 텍스트 파일 입출력 (`fscanf()`, `fprintf()`)

C언어에서는 `fscanf()`와 `fprintf()`를 사용하여 텍스트 파일에 데이터를 읽고 쓸 수 있습니다. 

- `fscanf()`는 파일에서 데이터를 읽어오는 함수입니다.
- `fprintf()`는 파일에 데이터를 출력하는 함수입니다.

### 예시

```c
#include <stdio.h>

int main() {
    FILE *file = fopen("example.txt", "w"); // 파일을 쓰기 모드로 열기
    if (file == NULL) {
        printf("파일을 열 수 없습니다.\n");
        return 1;
    }

    fprintf(file, "Hello, World!\n"); // 텍스트 파일에 문자열 출력

    fclose(file); // 파일 닫기

    // 파일 읽기
    file = fopen("example.txt", "r"); // 파일을 읽기 모드로 열기
    if (file == NULL) {
        printf("파일을 열 수 없습니다.\n");
        return 1;
    }

    char buffer[100];
    fscanf(file, "%s", buffer); // 텍스트 파일에서 문자열 읽기
    printf("파일에서 읽은 내용: %s\n", buffer);

    fclose(file); // 파일 닫기

    return 0;
}
```

---

## 2. 파일의 끝 (`feof`)

`feof()` 함수는 파일 포인터가 파일의 끝을 지났는지 확인하는 함수입니다. `feof()`는 파일을 읽을 때, 더 이상 읽을 데이터가 없을 경우 `true` 값을 반환합니다.

### 예시

```c
#include <stdio.h>

int main() {
    FILE *file = fopen("example.txt", "r"); // 파일을 읽기 모드로 열기
    if (file == NULL) {
        printf("파일을 열 수 없습니다.\n");
        return 1;
    }

    char ch;
    while ((ch = fgetc(file)) != EOF) { // 파일 끝까지 읽기
        putchar(ch); // 읽은 문자 출력
    }

    fclose(file); // 파일 닫기

    return 0;
}
```

---

## 3. 이진 파일 입력, 출력 (`fread()`, `fwrite()`)

이진 파일 입출력은 `fread()`와 `fwrite()`를 사용하여 수행할 수 있습니다. 이진 파일은 텍스트 파일과 달리 데이터가 그대로 저장되며, 텍스트 파일에서 사용하는 문자열이 아닌 바이너리 형식으로 데이터를 다룹니다.

- `fread()`는 파일에서 데이터를 읽는 함수입니다.
- `fwrite()`는 파일에 데이터를 쓰는 함수입니다.

### 예시

```c
#include <stdio.h>

typedef struct {
    int id;
    char name[20];
} Student;

int main() {
    FILE *file = fopen("students.dat", "wb"); // 이진 파일 쓰기 모드
    if (file == NULL) {
        printf("파일을 열 수 없습니다.\n");
        return 1;
    }

    Student student = {1, "John Doe"};
    fwrite(&student, sizeof(Student), 1, file); // 이진 파일에 구조체 쓰기

    fclose(file); // 파일 닫기

    // 파일 읽기
    file = fopen("students.dat", "rb"); // 이진 파일 읽기 모드
    if (file == NULL) {
        printf("파일을 열 수 없습니다.\n");
        return 1;
    }

    Student readStudent;
    fread(&readStudent, sizeof(Student), 1, file); // 이진 파일에서 구조체 읽기
    printf("ID: %d, Name: %s\n", readStudent.id, readStudent.name);

    fclose(file); // 파일 닫기

    return 0;
}
```

---

## 4. 랜덤 파일 접근법 (`fseek()`, `ftell()`, `rewind()`)

랜덤 파일 접근은 `fseek()`, `ftell()`, `rewind()` 함수를 사용하여 파일 내에서 특정 위치로 이동하거나 현재 위치를 확인할 수 있습니다.

- `fseek()`는 파일 포인터를 지정한 위치로 이동합니다.
- `ftell()`은 현재 파일 포인터의 위치를 반환합니다.
- `rewind()`는 파일 포인터를 파일의 시작으로 되돌립니다.

### 예시

```c
#include <stdio.h>

int main() {
    FILE *file = fopen("example.txt", "w+"); // 읽기/쓰기 모드로 파일 열기
    if (file == NULL) {
        printf("파일을 열 수 없습니다.\n");
        return 1;
    }

    fprintf(file, "Hello, World!\n");
    fseek(file, 0, SEEK_SET); // 파일 포인터를 시작으로 이동
    char ch;
    while ((ch = fgetc(file)) != EOF) {
        putchar(ch); // 파일 내용 출력
    }

    fseek(file, 0, SEEK_END); // 파일 끝으로 이동
    fprintf(file, "End of file.\n");

    rewind(file); // 파일 포인터를 처음으로 되돌리기

    fclose(file); // 파일 닫기

    return 0;
}
```

---

## 5. CSV, JSON 파일 저장

C언어에서 CSV 또는 JSON 파일을 다루는 것은 텍스트 파일을 다루는 방식과 비슷합니다. CSV는 콤마로 구분된 값들을 저장하는 형식이고, JSON은 구조화된 데이터를 텍스트 형식으로 저장하는 방식입니다.

### 예시 - CSV 저장

```c
#include <stdio.h>

int main() {
    FILE *file = fopen("data.csv", "w");
    if (file == NULL) {
        printf("파일을 열 수 없습니다.\n");
        return 1;
    }

    fprintf(file, "ID,Name,Age\n");
    fprintf(file, "1,John,25\n");
    fprintf(file, "2,Jane,30\n");

    fclose(file);

    return 0;
}
```

### 예시 - JSON 저장

```c
#include <stdio.h>

int main() {
    FILE *file = fopen("data.json", "w");
    if (file == NULL) {
        printf("파일을 열 수 없습니다.\n");
        return 1;
    }

    fprintf(file, "{\n");
    fprintf(file, "  \"name\": \"John\",\n");
    fprintf(file, "  \"age\": 25,\n");
    fprintf(file, "  \"city\": \"New York\"\n");
    fprintf(file, "}\n");

    fclose(file);

    return 0;
}
```

---

## 6. OCI 인터페이스 다운로드

OCI (Oracle Call Interface)는 Oracle 데이터베이스와 상호작용하기 위한 C 언어 기반의 API입니다. C에서 Oracle 데이터베이스와 연결하거나 쿼리를 실행하는 데 사용됩니다.

OCI 인터페이스를 사용하는 예시는 매우 복잡하며, 주로 Oracle 클라이언트 라이브러리를 설치하고, C 코드에서 OCI API를 호출하여 데이터베이스와 상호작용합니다. OCI 인터페이스의 사용 예시는 Oracle 공식 문서에서 확인할 수 있습니다.

OCI를 사용하는 C언어 코드 예시:

```c
#include <oci.h>
```

이 코드는 Oracle 데이터베이스와 연결하고 쿼리를 실행하는 과정을 포함합니다. 자세한 내용은 [Oracle Documentation](https://docs.oracle.com/en/database/oracle/oracle-database/)을 참고하십시오.

---

이와 같이 C언어는 다양한 방식으로 파일을 다룰 수 있는 기능들을 제공하며, 이를 통해 데이터를 효율적으로 저장하고 읽을 수 있습니다.
