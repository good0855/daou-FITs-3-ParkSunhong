

## 실습 26

```c
#include <stdio.h>

int main1() {

	int sum = 10, score[] = { 99, 80, 91, 78, 85 };
	int* psum, * pscore1, * pscore2;
	psum = &sum;
	pscore1 = score; // &score
	pscore2 = score; // &score
	++pscore2;

	pscore2++;

	printf("sum : %d\n", sum); // 10
	printf("*psum: %d\n", *psum); // 10
	printf("*psum+1: %d\n", *psum + 1); // 11
	printf("++*psum: %d\n", ++*psum); // 11
	printf("*(psum+1): %d\n", *(psum + 1));
	printf("*++psum: %d\n", *++psum);
	printf("===========================");
	printf("*pscore1: %d\n", *pscore1); // 99
	printf("*pscore1+1: %d\n", *pscore1 + 1); // 100
	printf("*(pscore1+1): %d\n", *(pscore1+1));  // 80
	printf("*pscore2-*pscore1: %d\n", *pscore2 - *pscore1); // -8
	printf("pscore2-pscore1: %d\n", pscore2 - pscore1);
	printf("pscore2: %d\n", pscore2);
	printf("pscore1: %d\n", pscore1);

	return 0;
}
```

## 실습 27

```c
#include <stdio.h>

int max(int, int);
int min(int, int);

int main() {
	char city[3][8] = { "seoul", "jeju", "busan" };
	char* pcity[] = { "seoul", "jeju", "busan" };

	printf("%s %s\n", city + 1, *(pcity + 1)); // pcity + 1 이렇게 하면 안 된다.
	printf("%s %s\n", city[1], *(pcity + 1)); // pcity + 1 이렇게 하면 안 된다.
	printf("%p %p", *(pcity + 1), pcity + 1); // pcity + 1 이렇게 하면 안 된다.
	
	int (*fpmm)(int, int); // 함수포인터 함수의 주소 저장
	int num1, num2, flag;
	printf("두개의 숫자를 입력 >> ");
	scanf_s("%d %d", &num1, &num2);
	
	printf("구하려는 값은 1. 최대, 2. 최소 >> ");
	scanf_s("%d", &flag);

	if (flag == 1) {
		fpmm = max;
		printf("max = %d\n", fpmm(num1, num2));
	}
	else {
		fpmm = min;
		printf("min = %d\n", fpmm(num1, num2));

	}

	return 0;
}

int max(int a, int b) {
	return (a > b) ? a : b;
}

int min(int a, int b) {
	return (a < b) ? a : b;
}

```
