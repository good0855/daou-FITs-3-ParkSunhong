# 2025-03-05-TIL

- 하드웨어 5대 장치
    - 입력장치
    - 출력장치
    - 제어장치 ⇒ 제어, 연산장치는 CPU
    - 연산장치
    - 기억장치
- 폰 노이만 아키텍처 프로그램 내장 방식
    - 모든 소프트웨어는 반드시 주기억장치 안에서 불러서 cpu 처리

## 레지스터

<aside>
💡

CPU가 연산을 처리하는 데 일시적으로 값을 저장하는 저장소

</aside>

- 프로그램 카운터((PC) : 다음에 인출할 명령어의 주소를 보유하고 있는 레지스터
- 누산기(AC) : 데이터 레지스터로 처리 결과를 임시로 보유하는 역할
- 명령어 레지스터(IR) : 현재 실행중인 명령어 저장하는 값
- 등등

---

## 참고

- 컴퓨터구조론

https://github.com/okskna/ComputerArchitecture_Summary

---

# C언어 기본

## `#pragma once`

`#pragma once`는 C와 C++에서 사용되는 **헤더 가드(Header Guard)**의 한 형태로, 헤더 파일이 여러 번 포함되는 것을 방지하는 역할을 합니다.

### 📌 `#pragma once`의 역할

헤더 파일이 중복 포함되면 같은 함수나 변수, 구조체가 여러 번 정의될 수 있어 **컴파일 오류**가 발생할 수 있습니다.

`#pragma once`를 사용하면 해당 헤더 파일이 한 번만 포함되도록 자동으로 처리됩니다.

- 내장된 헤더만`<>` 로 감쌀 수 있고, 사용자 정의 헤더는 `“”` 로 감싸야 한다.

```c
#include <stdio.h>
#include "day01.h"

int main() {

	printf("Hello World");
	return 0;
}
```

## 실행파일 순서

- 전처리기 → 컴파일러 → 어셈블러 → 링커 과정을 통해 실행파일이 메모리에 올라옴
- 메모리에 저장된 명령어는 CPU에 Fetch
- CPU에 fetch된 명령어 ⇒ 덧셈이라는 명령어로 decode
- Decode된 연산은 Execution된다.

## DLL 파일이란?

- DLL(Dynamic Link Library)
- 정적 라이브러리(.lib)와 비교
    - Compile 시점에 링커에 의해 Main 소스와 결합된다
    - 정적 라이브러리를 이용하는 경우에는 만들어진 실행파일만 전달하면 되기에 배포작업이 쉽지만, 실행파일 크기는 라이브러리에 따라 커질 수 있다.
- DLL
    - 컴파일 시점에 Main 코드와 결합되지 않는다.
    - 프로그램을 실행하면 dll 파일을 필요할 때 호출한다.
    - 실행파일 크기를 줄일 수 있다.
    - 단 dll파일 위치 중요

 

- **DLL파일 활용**
    
    **dll 파일을 활용하는 이유?**
    
    특정 프로그램이 모듈별 실행되는 경우라면 DLL 처리하면 좋다.
    
    [상황]
    
    - "A" 프로그램이 특정 DLL 파일 "B"가 필요하다.
    - "B" 파일은 핵심 Logic으로 전체 프로그램 성능을 좌우한다.
    - "A" 실행 프로그램은 변경점이 없으며, "B" 파일의 알고리즘에 따라 제품별 성능차이가 있다.
    
    [활용]
    
    → 실행파일 "A"와 DLL 파일 "B"가 분리되어 있기에 디버깅이 용이하다.
    
    → 정적 라이브러리 파일이 아니기에 "A"는 컴파일할 필요가 없다.
    
    → DLL 파일만 변경해서 배포 및 성능 테스트를 할 수 있다.
    
    → 기존 DLL 파일 "B"에 새로운 DLL 파일 "C"를 만들어서 성능을 높일 수 있다.
    
    → 여러 언로 버전으로 구성된 아키텍처에서 활용할 수 있다.
    

## `sizeof()`

C 언어에서 `sizeof` 연산자는 매우 중요한 연산자로, **자료형**이나 **변수**의 **크기(바이트 단위)**를 반환합니다. `sizeof`는 컴파일 시간에 크기를 계산하므로 프로그램 실행 중에 성능에 영향을 미치지 않습니다.

### **`sizeof` 연산자 기본 사용법**

`sizeof`는 두 가지 방식으로 사용될 수 있습니다:

1. **자료형을 인자로 넘기는 경우** (예: `sizeof(int)`)
2. **변수를 인자로 넘기는 경우** (예: `sizeof(x)`)

### **1. 자료형에 대한 `sizeof`**

자료형에 대해 `sizeof`를 사용하면, 해당 자료형의 크기를 바이트 단위로 반환합니다.

### 예시:

```c
#include <stdio.h>

int main() {
    printf("Size of int: %zu\n", sizeof(int));      // 4바이트 (대부분의 시스템)
    printf("Size of char: %zu\n", sizeof(char));    // 1바이트
    printf("Size of double: %zu\n", sizeof(double)); // 8바이트 (대부분의 시스템)
    printf("Size of float: %zu\n", sizeof(float));   // 4바이트
    return 0;
}

```

### `sizeof` 반환값의 타입

- `sizeof`는 항상 **`size_t`** 타입을 반환합니다. 이 타입은 **부호 없는 정수** 타입으로, 시스템에서 최대 크기만큼 표현할 수 있습니다.
- `size_t`는 보통 `unsigned int` 또는 `unsigned long` 타입으로 구현됩니다.

### **2. 변수에 대한 `sizeof`**

변수에 대해 `sizeof`를 사용하면, 해당 변수의 **메모리 크기**를 바이트 단위로 반환합니다. 변수의 타입에 따라 결과가 달라집니다.

### 예시:

```c
#include <stdio.h>

int main() {
    int a = 10;
    double b = 5.6;

    printf("Size of a: %zu\n", sizeof(a));  // a는 int형 변수
    printf("Size of b: %zu\n", sizeof(b));  // b는 double형 변수
    return 0;
}

```

### **3. 배열에 대한 `sizeof`**

배열에 대해 `sizeof`를 사용하면, **배열의 전체 크기**를 반환합니다. 배열의 크기는 **배열의 원소 개수 × 원소의 크기**입니다.

### 예시:

```c
#include <stdio.h>

int main() {
    int arr[10];
    printf("Size of arr: %zu\n", sizeof(arr));  // 10 * 4 (int의 크기)
    printf("Size of one element of arr: %zu\n", sizeof(arr[0]));  // 4 (int의 크기)
    return 0;
}

```

### 주의 사항:

- 배열이 **포인터로 변환**되면 `sizeof`가 예상한 대로 동작하지 않습니다.
- 배열의 크기를 변수로 넘길 때, 배열은 **포인터로 취급**되므로 `sizeof`를 사용해 크기를 구할 수 없습니다.

```c
#include <stdio.h>

void func(int arr[]) {
    printf("Size of arr in func: %zu\n", sizeof(arr));  // 포인터 크기만 반환
}

int main() {
    int arr[10];
    printf("Size of arr in main: %zu\n", sizeof(arr));  // 10 * 4 (int의 크기)
    func(arr);
    return 0;
}

```

### **4. 포인터에 대한 `sizeof`**

포인터에 대해 `sizeof`를 사용하면, **포인터 자체의 크기**를 반환합니다. 포인터의 크기는 **시스템 아키텍처에 따라 다르**며, 보통 32비트 시스템에서는 4바이트, 64비트 시스템에서는 8바이트입니다.

### 예시:

```c
#include <stdio.h>

int main() {
    int *p;
    double *q;

    printf("Size of int pointer: %zu\n", sizeof(p));    // 4바이트 (32비트 시스템) 또는 8바이트 (64비트 시스템)
    printf("Size of double pointer: %zu\n", sizeof(q)); // 4바이트 (32비트 시스템) 또는 8바이트 (64비트 시스템)
    return 0;
}

```

### **5. `sizeof`의 특이점**

- `sizeof`는 **컴파일 타임**에 계산되므로, 실행 중에 계산되는 것이 아니라 컴파일러가 미리 크기를 알 수 있는 정보를 기반으로 값을 반환합니다.
- **동적 할당된 메모리**에 대해서는 `sizeof`를 직접 사용할 수 없습니다. 예를 들어 `malloc`으로 할당한 메모리의 크기를 알 수 없습니다. 대신 `sizeof`는 포인터의 크기만 반환합니다.

### 예시:

```c
#include <stdio.h>
#include <stdlib.h>

int main() {
    int *p = (int *)malloc(10 * sizeof(int));  // 동적으로 10개의 int를 할당
    printf("Size of pointer p: %zu\n", sizeof(p));  // 포인터의 크기 (4바이트 또는 8바이트)
    printf("Size of allocated memory: %zu\n", 10 * sizeof(int));  // 10 * 4 (int 크기)
    free(p);
    return 0;
}

```

### **6. 구조체에 대한 `sizeof`**

구조체의 크기는 **구성 요소의 크기**를 모두 합친 것과 **패딩**을 고려하여 결정됩니다. 구조체 내의 멤버들이 메모리에서 **정렬 규칙에 맞춰** 배치되기 때문에, 실제 크기는 단순히 멤버 크기의 합보다 클 수 있습니다.

### 예시:

```c
#include <stdio.h>

struct Example {
    char a;   // 1 바이트
    int b;    // 4 바이트
    char c;   // 1 바이트
};

int main() {
    printf("Size of struct Example: %zu\n", sizeof(struct Example));  // 12 바이트 (패딩을 고려한 크기)
    return 0;
}

```

### **결론**

- `sizeof`는 **자료형**이나 **변수**의 **메모리 크기**를 바이트 단위로 반환하는 연산자입니다.
- **자료형, 변수, 배열, 포인터, 구조체** 등 다양한 데이터 유형에 대해 사용 가능하며, 특히 배열이나 구조체에서 중요한 역할을 합니다.
- **동적 메모리**나 **포인터**와 관련된 `sizeof`의 사용 시 주의점도 있으며, **패딩**과 **정렬 규칙**에 의한 구조체 크기의 차이를 이해하는 것이 중요합니다.

# 실습

## 실습3

```c
#include <stdio.h>

int main() {

	printf("================\n");
	printf("학과 : %s\n", "컴퓨터공학과");
	printf("이름 : %s\n", "박선홍");
	printf("학번 : %s\n", "4595");
	printf("강사 : %s\n", "정현희");
	printf("================\n");

	return 0;
}
```

```c
#include <stdio.h>

int main() {

	printf("================\n");
	printf("학과 : %s\n", "컴퓨터공학과");
	printf("이름 : %s\n", "박선홍");
	printf("학번 : %s\n", "4595");
	printf("강사 : %s\n", "정현희");
	printf("================\n");

	main2();

	return 0;
}

// 실습 02
int main2() {
	printf("================\n");
	printf("이름 : %s\n", "박선홍");
	printf("목표 : %s\n", "열심히 하겠습니다");
	printf("애칭 : %s\n", "없음");
	printf("================\n");
}
```

---

## Overflow 및 리터럴 우선순위

```c
#include <stdio.h>

int main() {
	

	int a;
	//unsigned int ua = 0;

	a = -2147483648;

	printf("%d", a);

	return 0;
}

```

- 위 상황에서는 에러가 남.
    
    ![image.png](attachment:07e4df9e-7ba4-4731-8129-1ffd6d614762:image.png)
    
- 이유 : 리터럴 우선순위
- C에서 기본적으로 정수 리터럴의 타입은 int
    - -2147483648은 `signed int` 의 최솟값(signed int 범위 : -2147483648 ~ 2147483647)
- 하지만 C에서는 `-2147483648`을 **단일 값**이 아니라 `-(2147483648)`로 해석
- 즉 `2147483648` 은 여기서 `signed int` 가 아니라 `unsigned int`로 해석된다(signed int형 범위를 초과하므로)
- 따라서 `-(2147483648)`는 `-(unsigned int)` 연산이 되어, **오버플로우가 발생 가능**
    - **부호 비트와 부호 없는 타입 간의 연산**으로 인해 예상치 못한 **오버플로우가 발생**

- 해결
    - 오버플로우 문제를 방지하려면 **`-2147483648`을 `long` 타입으로 명시적으로 지정**하는 게 좋다
    - 혹은 명시적 형변환 사용

```c
#include <stdio.h>

int main() {
    long a = -2147483648L;  // 명시적으로 long 타입 지정
    printf("%ld", a);
    return 0;
}

```

```c
#include <stdio.h>

int main() {
    int a = (int) -2147483648;
    printf("%d", a);
    return 0;
}
```

- ascii 실습

```c
#include <stdio.h>

int main() {
	

	
	char null = 0;
	char beep = 7;
	char backspace = 8;
	char tab = 9;
	char line_feed = 10;
	char double_quote = 34;
	char single_quote = 96;
	char bar = 92;

	printf("null: %c\n", null);
	printf("beep: %c\n", beep);
	printf("backspace: %c\n", backspace);
	printf("tab: %c\n", tab);
	printf("line_feed: %c\n", line_feed);
	printf("double_quote: %c\n", double_quote);
	printf("single_quote: %c\n", single_quote);
	printf("bar: %c\n", bar);

	return 0;
}

```

## 입출력

```c
int main() {
	int aaa;
	printf("정수를 입력하세요 >> ");

	scanf_s("%d", &aaa); // aaa변수의 메모리 주소에 값을 넣어달라고 명시적으로 표현!

	printf("%d", aaa);
}
```
