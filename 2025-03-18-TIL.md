# 메모리 구조

![image.png](attachment:03813b62-ff9a-46cd-b6e0-35b643aeb0a2:image.png)

- Text Segment
    - 컴파일되어 바이너리 코드(기계어) 형태로 저장된 영역
        - 실행 코드(기계어 저장), 낮은 메모리 주소
        - 프로그램의 모든 함수와 명령어가 포함된다.
    - 읽기 전용으로 설정되어 실행 중 코드 수정 불가능
    - 코드에 직접적으로 적혀있는 **문자열(String)과 상수** 또한 코드 영역에 저장됨
        - 프로그램 실행 시 절대 변경되면 안되는 데이터를 저장 (코드, 상수, 코드 내 문자열 등)
- Data Segment
    - 초기화된 전역변수 및 정적 변수 저장
    - 초기값이 있는 전역, 정적 변수가 저장
    - **프로그램 시행 전**에 메모리에 로드된다.
- BSS Segment
    - 초기화되지 않은 전역 변수 및 정적(static) 변수 저장
- Heap
    - 동적으로 할당되는 메모리영역(malloc, calloc, realloc, free)
- Stack
    - 함수 호출시 생성되는 지역변수, 높은 메모리 주소

![image.png](attachment:5acff4c9-b227-49a0-a736-32f50cf0d900:image.png)

- string literal은 어디에 저장이 될까? ⇒  **read-only segment**
    - Data Segment or Text Segment
    - 플랫폼 등 상황에 따라 다름
    
    https://www.quora.com/Where-are-strings-stored-in-C
    

---

# `malloc()`

- Memory Allocation함수는 C언어에서 동적 메모리 할당함
- 빠르게 메모리 할당, 초기화 불필요하는 경우(calloc()과 반대)
- 실시간(런타임) 동안 힙 영역에 메모리 할당
- 초기화되지 않는 메모리 블록의 포인터 반환
- 할당된 메모리는 반드시 `free()` 를 사용하여 해제

```c
void* malloc() (size_t size)
// 여기서 void*는 어떤 값으로도 casting할 수 있다는 의미이다.
// 메모리 할당이 성공하면 반환값으로 할당된 메모리 블록의 시작 주소
// 실패하면 NULL 반환

// ex. 정수 4byte만큼 동적 할당 하여서 값을 넣고 출력하기
int* ptr = (int*)malloc(sizeof(int)); // int크기만큼 동적할당
```

- 예제

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <malloc.h>

int main() {
	int* ptr = (int*)malloc(sizeof(int));

	if (ptr == NULL) {
		printf("메모리 할당 실패\n");
		return 1;
	}

	*ptr = 42; // int인 42 올리기
	*ptr += 100;

	if (*ptr >= 100) printf("합격\n");
	else printf("불합격\n");

	free(ptr);

	double* ptr2 = (double*)malloc(sizeof(double));
	*ptr2 = 3.14;
	printf("double ; %.lf\n", *ptr2);
	free(ptr2);

	// 실수 4개 동적 배려
	int size = 4;
	double* ptr3 = (double*)malloc(sizeof(double) * size);
	*ptr3 = 1.1;
	*(ptr3 + 1) = 2.2;
	*(ptr3 + 2) = 3.3;
	*(ptr3 + 3) = 4.4;

	for (int i = 0; i < size; i++) {
		printf("num: %.2lf\n", ptr3[i]);
	}

	free(ptr3);

}
```

---

# `calloc()`

- 0으로 초기화 한다.
- 다중 요소 할당 시 안전하게 초기화 할 경우
- 되도록 size를 잡을 때면 4의 배수단위로 하자
    - size = 10이면 10만큼 할당하는 것이 아니라 12 할당하기 때문

```c
void* calloc(size_t_num, size_t size)
// size를 num개만큼 0으로 초기화
```

## malloc() vs calloc() 초기화 값 비교

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>

int main() {
	int n = 5;
	int* arr1 = (int*)malloc(n * sizeof(int));
	int* arr2 = (int*)calloc(n, sizeof(int));

	printf("malloc()사용 : ");
	for (int i = 0; i < n; i++) {
		printf("%d ", arr1[i]);
	}
	putchar('\n');

	printf("calloc()사용 : ");
	for (int i = 0; i < n; i++) {
		printf("%d ", arr2[i]);
	}
	putchar('\n');

	free(arr1);
	free(arr2);

	

}

>>
malloc()사용 : -842150451 -842150451 -842150451 -842150451 -842150451
calloc()사용 : 0 0 0 0 0
```

## (제출) 2차원 배열을 할당하는 문제

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>

int main() {
	
	int rows = 3, cols = 4;
	// 2차원 배열 동적할당
	int** arr = (int**)calloc(rows, sizeof(int*));

	for (int i = 0; i < rows; i++) {
		arr[i] = (int*)calloc(cols, sizeof(int));
	}

	// 값 입력과 출력
	for (int i = 0; i < rows; i++) {
		for (int j = 0; j < cols; j++) {
			arr[i][j] = i * cols + j;
			printf("%d ", arr[i][j]);
		}
		printf("\n");
	}

	// 메모리 해제
	for (int i = 0; i < rows; i++) {
		free(arr[i]);
	}

	free(arr);
	return 0;

	

}
```

---

# `realloc` 함수

- 이미 할당된 메모리 블록의 크기를 축소
- 크기를 확장할 때, 기존 데이터를 그대로 유지하며 새로운 메모리 블록 할당
- **(중요) 기존 메모리 크기를 변경하면서 새로운 메모리 주소를 반환할 수 있기 때문에, 변경된 포인터를 반드시 저장해야 한다.**

```c
void* realloc(void* ptr, size_t, size);
// ptr = 기존에 할당된 메모리 블록의 포인터
// size = 새로운 메모리 블록의 크기(바이트 단위)
// 반환값 - 성공하면 새로운 메모리 블록의 포인터, 실패하면 NULL
```

- 코드
    - 기존의 코드

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>

int main() {
	
	int n = 5;
	int* arr = (int*)malloc(n * sizeof(int));

	if (arr == NULL) {
		printf("메모리할당 실패.\n");
		return 1;
	}

	for (int i = 0; i < n; i++) {
		arr[i] = i + 1;
	}
	
	printf("초기 배열");
	for (int i = 0; i < n; i++) {
		printf("%d ", arr[i]);
	}
	printf("\n");

	// 메모리 크기 변경
	n = 10;
	int* new_arr = (int*)realloc(arr, n * sizeof(int));

	if (new_arr == NULL) {
		printf("메모리할당 실패\n");
		free(arr); // 기존 메모리 해제
		return 1;
	}

	arr = new_arr;
	for (int i = 5;i < n;i++) {
		arr[i] = (i + 1) * 10;

	}

	printf("크기변경 후 배열");
	for (int i = 0; i < n; i++) {
		printf("%d ", arr[i]);
	}
	printf("\n");
	
	// 이미 arr을 해제함 => 같은 주소의 값을 해제하면 오류!
	free(arr);
	//free(new_arr);

	return 0;

}
```

- 주의점
    - 메모리 할당에 실패할 때, NULL 반환
    - 이때 기존 포인터가 NULL로 변경되면 안되므로 realloc 하기 전에 따로 변수 저장
    - 예시
        
        ### ✅ **`realloc()`에서 메모리 할당이 실패하면 어떻게 될까?**
        
        ### 📌 **실패 시 동작**
        
        1. `realloc()`이 **메모리 할당에 실패하면** `NULL`을 반환한다.
        2. 하지만, 기존 `ptr`은 그대로 유지되며, 원래의 메모리는 **해제되지 않는다.**
        3. 따라서, **반환값을 확인하지 않고 기존 포인터를 덮어쓰면 메모리 누수**가 발생할 수 있다.
        
        ---
        
        ### 🔹 **잘못된 사용 예 (메모리 누수 발생)**
        
        ```c
        arr = realloc(arr, 1000000 * sizeof(int));  // ❌ 위험한 코드
        
        ```
        
        - 할당이 실패하면 `realloc()`은 `NULL`을 반환하지만, `arr`이 덮어써지기 때문에 기존 메모리를 해제할 방법이 없음.
        - **결과:** 메모리 누수(memory leak) 발생!
        
        ---
        
        ### 🔹 **안전한 사용 예 (임시 포인터 활용)**
        
        ```c
        int* temp = realloc(arr, 1000000 * sizeof(int));
        if (temp) {
            arr = temp;  // ✅ 성공하면 arr을 새로운 주소로 변경
        } else {
            printf("Memory allocation failed!\n");
            // ✅ 기존 arr은 그대로 유지됨 -> 해제할 수 있음
        }
        
        ```
        
        - 할당이 실패하면 기존 `arr`은 그대로 유지되므로 **데이터 손실을 방지**할 수 있다.
        
        ---
        
        ### 🔹 **완전한 예제**
        
        ```c
        #include <stdio.h>
        #include <stdlib.h>
        
        int main() {
            int* arr = (int*)malloc(3 * sizeof(int));  // 초기 3개 크기 할당
            if (!arr) {
                printf("Initial memory allocation failed!\n");
                return 1;
            }
        
            // 기존 데이터 입력
            for (int i = 0; i < 3; i++) arr[i] = i + 1;
        
            // 메모리 확장 (실패 처리)
            int* temp = (int*)realloc(arr, 1000000000 * sizeof(int));  // 큰 크기 할당 시도
            if (temp) {
                arr = temp;  // ✅ 성공 시 업데이트
            } else {
                printf("Memory reallocation failed! Keeping original allocation.\n");
                // ✅ 기존 메모리 유지됨 (arr 사용 가능)
            }
        
            // 기존 데이터 출력
            for (int i = 0; i < 3; i++) {
                printf("%d ", arr[i]);
            }
        
            // 메모리 해제
            free(arr);
        
            return 0;
        }
        
        ```
        
        ---
        
        ### 🔹 **결론**
        
        ✔ **할당 실패 시 반드시 `NULL` 체크**
        
        ✔ **임시 포인터를 활용해 기존 데이터 보호**
        
        ✔ **메모리 누수를 방지하려면 `free()`를 반드시 호출**
        
    

### 🔹 **메모리 크기 조정 시 `realloc()` 동작**

| 크기 변화  | 동작 원리 |
| --- | --- |
| 🔼 크기 증가 | 기존 블록에 여유 공간 있으면 **그대로 확장** |
|  | 공간 부족하면 **새 블록 할당 + 기존 데이터 복사** |
| 🔽 크기 감소 | 기존 블록에서 **크기만 조정 (주소 유지 가능)** |
|  | 일부 시스템에서는 **새 블록 할당 후 데이터 복사** |
|  |  |
|  |  |

---

# C에서의 변수 할당과 구조체 복사

맞습니다! Java와 C의 주요 차이점 중 하나가 **값 복사**와 **참조** 처리 방식입니다. C에서는 기본적으로 구조체와 같은 복잡한 데이터 타입을 **값으로 복사**하는 방식을 사용하며, Java에서는 **참조**를 사용하여 객체를 다루기 때문에 차이가 있습니다. 이 차이를 좀 더 명확히 설명하겠습니다.

### 1. **C에서 구조체 복사 (값 복사)**:

C에서는 구조체가 기본적으로 **값으로 복사**됩니다. 즉, `a = customers[i].transactions[j]`와 같은 코드에서 `a`는 `customers[i].transactions[j]`의 **복사본**이 됩니다. 따라서 `a`를 수정해도 `customers[i].transactions[j]`는 변화하지 않습니다.

**예시**:

```c
Transaction a = customers[i].transactions[j];  // 복사됨
a.price = 1000;  // a의 price를 수정해도 customers[i].transactions[j]는 바뀌지 않음

```

### 2. **Java에서 객체 참조 (Reference)**:

Java에서는 객체를 다룰 때 기본적으로 **참조**를 사용합니다. 즉, `a = customers[i].transactions[j]`라고 하면, `a`는 `customers[i].transactions[j]` 객체의 **참조**를 가리키게 되며, 두 변수는 동일한 객체를 가리키게 됩니다. 따라서 하나의 변수를 수정하면 다른 변수에도 반영됩니다.

**예시**:

```java
Transaction a = customers[i].transactions[j];  // 참조가 전달됨
a.price = 1000;  // a의 price를 수정하면 customers[i].transactions[j]도 바뀜

```

### 3. **C에서 참조 (포인터 사용)**:

C에서도 참조 개념을 사용할 수 있는데, 이때 **포인터**를 사용합니다. 포인터는 변수의 메모리 주소를 가리키기 때문에, 포인터를 이용하면 Java의 참조와 유사한 방식으로 객체를 다룰 수 있습니다.

**예시**:

```c
Transaction* a = &customers[i].transactions[j];  // 포인터로 참조
a->price = 1000;  // a의 price를 수정하면 customers[i].transactions[j]도 바뀜

```

이 경우 `a`는 `transactions[j]`의 **포인터**로, `a->price`를 수정하면 `customers[i].transactions[j]`에도 영향을 미칩니다. 이는 Java에서 참조를 사용하는 방식과 유사합니다.

### 4. **정리**:

- **C에서 기본값 복사**: 구조체 변수는 기본적으로 값을 복사하여 새로운 변수에 저장합니다. 따라서 두 변수는 독립적으로 존재합니다.
- **C에서 포인터를 사용한 참조**: C에서 **포인터**를 사용하면 Java에서처럼 참조를 전달할 수 있습니다. 포인터는 변수의 메모리 주소를 가리키기 때문에, 이를 통해 값을 수정하면 원본 데이터에도 영향을 미칩니다.
- **Java에서 참조**: Java에서 객체를 변수에 할당하면 그 변수는 객체의 참조를 가리키게 되며, 값을 수정하면 원본 객체에 반영됩니다.

따라서 **C에서는 기본적으로 값 복사**가 이루어지고, **포인터를 사용하면 참조**처럼 동작할 수 있습니다.
