# 2025-03-19-TIL

---

# 파일 시스템 및 파일 처리 비교

## 1. 파일 시스템 개요

운영체제에서 파일을 관리하기 위해 파일 시스템을 사용합니다.

- **Windows**: FAT32, NTFS, exFAT
- **Mac**: exFAT, APFS
- **Linux**: ext4

---

## 2. C언어와 Java 언어의 파일 처리 비교

C언어와 Java는 파일을 처리하는 방식에서 여러 공통점과 차이점이 있습니다.

### 1) 공통점

- **파일 입출력 기능 제공**
    - C언어: `fopen`, `fgetc`, `fputc`, `fread`, `fwrite` 등
    - Java: `FileInputStream`, `FileOutputStream`, `BufferedReader`, `BufferedWriter` 등
- **파일 열기/닫기**
    - C언어: `fopen()`으로 파일을 열고, `fclose()`로 파일을 닫음
    - Java: `FileInputStream`, `FileOutputStream`을 사용하며 `close()` 메서드로 닫음
- **예외 처리**
    - C언어: 파일 오류를 `NULL` 반환으로 처리하며, 명시적으로 오류 상태를 확인해야 함
    - Java: `IOException`을 활용한 예외 처리 메커니즘 제공
- **텍스트 및 바이너리 파일 처리**
    - 두 언어 모두 텍스트 파일과 바이너리 파일을 읽고 쓸 수 있음

### 2) 차이점

### (1) 파일 처리 방식

- **C언어**: 파일 포인터(`FILE*`)를 사용하여 직접 관리
- **Java**: 스트림(`Stream`) 기반의 파일 입출력 제공

### (2) 입출력 방식

- **C언어**: 저수준의 파일 입출력 방식 제공 (배열 또는 버퍼를 사용하여 수동 처리)
- **Java**: 고수준의 파일 입출력 방식 제공 (버퍼링 기능을 활용한 효율적인 입출력 처리)

### (3) 예외처리 방식

- **C언어**: `NULL` 반환 여부 확인
    
    ```c
    FILE *fp = fopen("file.txt", "r");
    if (fp == NULL) {
        printf("파일 열기 실패\n");
    }
    
    ```
    
- **Java**: `try-catch`를 활용한 예외 처리
    
    ```java
    try {
        FileInputStream fis = new FileInputStream("file.txt");
    } catch (FileNotFoundException e) {
        e.printStackTrace();
    }
    
    ```
    

### (4) 파일 시스템과 스트림 처리

- **C언어**: 파일 시스템에 대한 직접적인 접근을 제공
- **Java**: 파일 스트림을 활용한 추상화 제공

### (5) 버퍼링 지원

- **C언어**: 수동 버퍼 관리 필요
- **Java**: `BufferedReader`, `BufferedWriter` 등의 클래스를 활용하여 자동 버퍼링 지원

---

## 3. 파일 처리에서 알아야 할 용어

### 1) 스트림(Stream)

- 프로그램과 입출력 장치 사이에서 데이터를 연결하는 역할을 함

### 2) C언어의 표준 입출력 스트림

| 스트림 | 설명 |
| --- | --- |
| `stdin` | 표준 입력 스트림 (키보드 입력) |
| `stdout` | 표준 출력 스트림 (모니터 출력) |
| `stderr` | 표준 에러 출력 (모니터 출력) |

### 3) 버퍼(Buffer)

- 스트림 내부에 존재하는 임시 메모리 공간
- `fflush(FILE *stream);` 함수를 사용하여 출력 버퍼를 비울 수 있음

### 4) 스트림의 종류

### (1) 사용 목적에 따른 분류

- 읽기 스트림
- 쓰기 스트림

### (2) 대상에 따른 분류

- 텍스트 데이터 스트림
- 바이너리 데이터 스트림

### (3) 데이터 이동 방향에 따른 분류

| 스트림 종류 | 설명 |
| --- | --- |
| `READ` 스트림 | 읽기 전용 스트림 |
| `WRITE` 스트림 | 쓰기 전용 스트림 |
| `APPEND` 스트림 | 기존 파일 끝에 데이터를 추가 |
| `READ/WRITE` 스트림 | 읽기 및 쓰기 모두 가능 |

### (4) 파일 개방 모드

| 모드 | 설명 |
| --- | --- |
| `r` | 읽기 모드 |
| `w` | 쓰기 모드 (파일이 없으면 생성) |
| `a` | 추가 모드 (기존 파일 끝에 추가) |
| `r+` | 읽기 및 쓰기 모드 |
| `w+` | 읽기 및 쓰기 모드 (기존 내용 삭제) |
| `a+` | 읽기 및 추가 모드 |

---

## 4. 파일 관련 주요 함수 정리

### `<stdio.h>` (C언어 표준 입출력 헤더)

- **파일 열기**: `FILE *fopen(const char *filename, const char *mode);`
- **파일 닫기**: `int fclose(FILE *stream);`
- **파일 읽기/쓰기**
    - `size_t fread(void *ptr, size_t size, size_t count, FILE *stream);`
    - `size_t fwrite(const void *ptr, size_t size, size_t count, FILE *stream);`
    - `char *fgets(char *str, int n, FILE *stream);`
    - `int fputs(const char *str, FILE *stream);`
- **문자 단위 입출력**
    - `int fgetc(FILE *stream);`
    - `int fputc(int character, FILE *stream);`
- **파일 포맷 입출력**
    - `int fprintf(FILE *stream, const char *format, ...);`
    - `int fscanf(FILE *stream, const char *format, ...);`
- **기타 기능**
    - `int feof(FILE *stream); // EOF 확인`
    - `int fseek(FILE *stream, long offset, int origin); // 파일 위치 이동`

---

## 5. 참고 자료

- [Sejong Library Guide](https://sejong-kr.libguides.com/c.php?g=942235&p=6822370)
