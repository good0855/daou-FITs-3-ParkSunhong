# 2025-03-07-TIL

## `_getch()`

- **char형 한 글자를 입력받기 위한 함수**이고, **입력받은 값을 가져와 아스키코드값으로 반환**해줍니다.
    - (값이 없을경우 null을 반환)
    - **콘솔 비표준 입출력 함수**를 사용하기 때문에 **표준 입력 버퍼에 저장되지 않고** 입력된 값을 바로바로 반환
- `conio.h` 헤더파일을 자신의 소스 코드에 include

---

### 무슨 차이인지?

- `getchar()`, `putchar()`, `scanf()` , `printf()` 는 **콘솔 표준 입출력 함수**
- `getch()` , `putch()` 등 함수는 **콘솔 비표준 입출력 함수**
- 입출력 버퍼를 사용하느냐 사용하지 않느냐에 따라 다름

- 다음 코드는 windows에서는 종료가 안되고 linux환경에서는 종료가 된다.
- 아마 표준 입출력(Standard I/O) 및 스트림(Stream) 동작 방식은 운영체제마다 달라서 생긴 이슈

```c
#include <stdio.h>

int main() {
    char ch;

    while (1) {
        printf("키 입력 (ESC(27) 입력 시 종료) >> ");
        ch = getchar();  // 한 문자 입력

        if (ch == 27) {  // ESC 입력 시 종료
            break;
        }
        
        getchar(); // 버퍼에 남아 있는 '\n' 제거

        printf("입력한 키: %c (%d)\n", ch, ch);
    }

    printf("프로그램 종료\n");
    return 0;
}
```

- 만약 콘솔 비표준 함수를 사용한다면
- windows에서는 비표준 입력을 처리하기 위해 `conio.h` 사용

```c
#include <stdio.h>
#include <conio.h>

int main() {
    char ch;
    printf("아무 키나 누르세요 (Esc = 종료)\n");

    while (1) {
        ch = _getch();  // Enter 없이 키 입력 받기
        printf("입력된 키: %d\n", ch);  // ASCII 코드 출력

        if (ch == 27)  // Esc (ASCII 27)이면 종료
            break;
    }

    return 0;
}

```

- linux, unix에서는 이렇게 처리함 ⇒ 버퍼를 사용하지 않는 콘솔 비표준 입출력을 사용하기 위해서는 수동으로 버퍼 비활성화 해줘야함!

```c
#include <stdio.h>
#include <termios.h>
#include <unistd.h>

// 버퍼링 비활성화
void disableBuffering() {
    struct termios t;
    tcgetattr(STDIN_FILENO, &t);
    t.c_lflag &= ~ICANON;  // 라인 버퍼링 해제
    t.c_lflag &= ~ECHO;    // 입력값 화면에 표시 안 함
    tcsetattr(STDIN_FILENO, TCSANOW, &t);
}

int main() {
    disableBuffering();  // 터미널 설정 변경
    char ch;

    printf("아무 키나 누르세요 (Esc = 종료)\n");

    while (1) {
        ch = getchar();  // Enter 없이 입력 받기
        printf("입력된 키: %d\n", ch);

        if (ch == 27)  // Esc (ASCII 27)이면 종료
            break;
    }

    return 0;
}

```

- 참고 :
    - https://pgh268400.tistory.com/357
    - https://growup-dev.tistory.com/entry/CC-getch-%ED%95%A8%EC%88%98

---

## variadic functions

- `...` 을 사용하면 된다.
- `stdarg.h` 를 사용하면 윈도우 말고도 리눅스, 유닉스에서도 안전하게 처리할 수 있음

```c
#include <stdio.h>
#include <stdarg.h>

// 가변 인자를 받는 함수 선언
int sum(int count, ...);

int main() {
	int result = sum(3, 1, 2, 3);
	printf("sum: %d\n", result);
	return 0;
}

// 가변 인자를 받는 함수 정의
int sum(int count, ...) {
	va_list args;
	va_start(args, count);

	int result = 0;
	for (int i = 0; i < count; ++i) {
		result += va_arg(args, int);
	}

	va_end(args);
	return result;
}
```

물론이죠! `va_list`, `va_start`, `va_arg`, `va_end`는 가변 인자를 처리하기 위해 사용되는 매크로입니다. 각각의 매크로의 역할에 대해 자세히 설명해드릴게요.

1. **`va_list`**
    - `va_list`는 가변 인자를 저장하는 형식입니다. 이 형식을 사용하여 가변 인자를 처리하기 위한 포인터를 선언합니다.
    
    ```c
    va_list args;
    
    ```
    
2. **`va_start`**
    - `va_start`는 가변 인자의 시작을 설정하는 매크로입니다. 이 매크로는 가변 인자 목록을 초기화하며, 첫 번째 인수는 `va_list` 형식의 변수, 두 번째 인수는 마지막 고정 인자입니다.
    
    ```c
    va_start(args, count); // 'args'는 'va_list' 변수, 'count'는 마지막 고정 인자
    
    ```
    
3. **`va_arg`**
    - `va_arg`는 가변 인자 목록에서 다음 인자를 가져오는 매크로입니다. 첫 번째 인수는 `va_list` 형식의 변수, 두 번째 인수는 가져올 인자의 형식입니다. 이 매크로는 인자를 가져온 후 포인터를 다음 인자로 이동시킵니다.
    
    ```c
    int value = va_arg(args, int); // 'args'는 'va_list' 변수, 'int'는 가져올 인자의 형식
    
    ```
    
4. **`va_end`**
    - `va_end`는 가변 인자 처리가 끝났음을 알리는 매크로입니다. 이 매크로는 가변 인자 목록을 정리하고, 초기화된 포인터를 무효화합니다.
    
    ```c
    va_end(args); // 'args'는 'va_list' 변수
    
    ```
    

이 매크로들을 사용하면 가변 인자를 처리할 수 있습니다. 예를 들어, 여러 개의 인자를 받아서 합을 계산할 때 유용합니다.
