## 2025-02-25-TIL

```java
package org.example.autoserviceproject.client;

import java.io.BufferedReader;
import java.io.InputStreamReader;
import java.net.HttpURLConnection;
import java.net.URL;
import java.net.URLEncoder;
import java.nio.charset.StandardCharsets;
import java.util.Properties;
import java.io.FileInputStream;
import java.io.IOException;

public class KISClient {
    private static final String CURRENT_PRICE_URL = "https://openapi.koreainvestment.com:9443/uapi/domestic-stock/v1/quotations/inquire-overtime-price";

    private static String ACCESS_TOKEN;
    private static String APP_KEY;
    private static String APP_SECRET;

    // til: static 블록에서 설정 파일을 로드하여 API 키를 설정한다.
    static {
        Properties properties = new Properties();
        try (FileInputStream fis = new FileInputStream("config.properties")) {
            properties.load(fis);
            ACCESS_TOKEN = properties.getProperty("access.token");  // til: access token을 읽어옴
            APP_KEY = properties.getProperty("app.key");  // til: application key를 읽어옴
            APP_SECRET = properties.getProperty("app.secret");  // til: app secret을 읽어옴
        } catch (IOException e) {
            e.printStackTrace();
            throw new RuntimeException("Failed to load configuration properties", e);  // til: 파일 로딩 실패 시 예외 처리
        }
    }

    /**
     * til: 주식의 현재 가격을 조회하는 메서드
     * @param marketCode - 시장 코드 (예: "J" - 주식, "W" - ELW)
     * @param stockCode  - 종목 코드 (예: "005930" - 삼성전자)
     * @return - 주식 가격 응답 (JSON 형식)
     * @throws Exception - 예외 처리
     */
    public String getCurrentPrice(String marketCode, String stockCode) throws Exception {
        // til: 요청 URL에 필요한 쿼리 파라미터를 인코딩하여 추가
        String requestUrl = CURRENT_PRICE_URL + "?"
                + "FID_COND_MRKT_DIV_CODE=" + URLEncoder.encode(marketCode, StandardCharsets.UTF_8)  // til: 시장 코드 인코딩
                + "&FID_INPUT_ISCD=" + URLEncoder.encode(stockCode, StandardCharsets.UTF_8);  // til: 종목 코드 인코딩

        URL url = new URL(requestUrl);  // til: 최종 요청 URL 생성
        HttpURLConnection conn = (HttpURLConnection) url.openConnection();
        conn.setRequestMethod("GET");  // til: HTTP GET 방식으로 요청 설정

        // til: 요청 헤더에 API 토큰과 키를 추가
        conn.setRequestProperty("Authorization", ACCESS_TOKEN);  // til: Authorization 헤더에 토큰 추가
        conn.setRequestProperty("appkey", APP_KEY);  // til: appkey 헤더에 appkey 추가
        conn.setRequestProperty("appsecret", APP_SECRET);  // til: appsecret 헤더에 appsecret 추가

        // 응답 처리
        int responseCode = conn.getResponseCode();  // til: 응답 코드 확인
        if (responseCode == 200) {  // til: 성공적인 응답인 경우
            BufferedReader in = new BufferedReader(new InputStreamReader(conn.getInputStream()));  // til: 응답 내용 읽기
            String inputLine;
            StringBuilder response = new StringBuilder();

            while ((inputLine = in.readLine()) != null) {  // til: 응답을 한 줄씩 읽어서 누적
                response.append(inputLine);
            }
            in.close();
            return response.toString();  // til: 최종 응답을 JSON 형식으로 반환
        } else {
            throw new RuntimeException("Failed to fetch current price. HTTP code: " + responseCode);  // til: 실패 시 예외 처리
        }
    }
}
```

### 클래스 구조 및 주요 내용

1. **상수 정의**:
   - `CURRENT_PRICE_URL`: 주식 가격을 조회하는 한국투자증권 API의 URL을 정의한 상수입니다.
   - `ACCESS_TOKEN`, `APP_KEY`, `APP_SECRET`: API를 사용할 때 필요한 인증 정보를 저장하는 변수들입니다. 이 값들은 설정 파일에서 읽어오도록 되어 있습니다.

2. **static 블록**:
   - `static` 블록에서는 클래스가 로드될 때 설정 파일에서 API 키와 토큰을 읽어오는 과정입니다.
   - `config.properties` 파일에서 API 인증 정보 (`access.token`, `app.key`, `app.secret`)를 읽어와서 `ACCESS_TOKEN`, `APP_KEY`, `APP_SECRET`에 저장합니다.
   - 설정 파일 로드에 실패할 경우 예외를 발생시킵니다.

3. **getCurrentPrice 메서드**:
   - 주식의 현재 가격을 조회하는 메서드입니다.
   - `marketCode`와 `stockCode`를 파라미터로 받아, 한국투자증권 API에 요청을 보냅니다.
     - `marketCode`: 예를 들어, "J"는 주식, "W"는 ELW(주식워런트증권) 등을 의미합니다.
     - `stockCode`: 종목 코드로, 예를 들어 삼성전자의 종목 코드 "005930"을 사용합니다.
   - URL에 파라미터를 인코딩하여 쿼리 문자열을 만듭니다. 이를 통해 요청 URL을 생성합니다.
   - HTTP GET 요청을 보내고, 응답 코드를 확인합니다.
     - 응답 코드가 `200`일 경우 정상 응답이며, 응답 본문을 JSON 형식으로 반환합니다.
     - 응답 코드가 다른 값일 경우 예외를 발생시킵니다.

### 기능 흐름
1. **설정 파일 로드**:
   - API 인증 정보는 외부 설정 파일(`config.properties`)에서 로드됩니다. 이 파일에는 한국투자증권 API의 `access.token`, `app.key`, `app.secret` 값이 포함되어 있어야 합니다.

2. **API 호출**:
   - `getCurrentPrice` 메서드는 주식의 현재 가격을 확인하기 위해 API에 요청을 보냅니다.
   - 주식의 **시장 코드**와 **종목 코드**를 사용하여 URL을 구성하고, 그 URL에 GET 요청을 보냅니다.

3. **응답 처리**:
   - API의 응답 코드가 `200`인 경우, 응답 본문을 JSON 형식으로 읽어 반환합니다.
   - 만약 응답 코드가 다른 값이라면, 요청이 실패했음을 나타내는 예외를 발생시킵니다.

### 설정 파일 예시
설정 파일(`config.properties`)은 아래와 같은 형식으로 작성되어야 합니다.

```properties
access.token=Bearer eyJ..........8GA
app.key=your_app_key
app.secret=your_app_secret
```

이 파일은 `KISClient` 클래스가 로드될 때 읽어져, API 호출을 위한 인증 정보를 제공합니다.

### API 호출 예시

예를 들어, 삼성전자의 주식 가격을 조회하려면 `getCurrentPrice` 메서드를 아래와 같이 호출할 수 있습니다:

```java
KISClient client = new KISClient();
String marketCode = "J";  // 주식 시장 코드
String stockCode = "005930";  // 삼성전자 종목 코드
String response = client.getCurrentPrice(marketCode, stockCode);
System.out.println("Current Price Response: " + response);
```
다.
- 설정 파일에는 API 키와 토큰 정보가 저장되어 있어야 하며, API 호출 시 이 정보를 헤더에 포함시켜 인증을 처리합니다.
